export const remainingCourses = [
  {
    id: 'php', title: 'PHP', description: 'Server-side scripting language powering 77% of the web.',
    officialDocs: 'https://www.php.net/docs.php', tutorialLink: 'https://www.w3schools.com/php/', exerciseLink: 'https://www.w3schools.com/php/php_exercises.asp',
    sections: [
      { title: 'What is PHP', content: `PHP (PHP: Hypertext Preprocessor) is a widely-used, open-source server-side scripting language. It powers approximately 77% of all websites, including WordPress, Facebook, and Wikipedia.\n\nPHP code is embedded within HTML and executed on the server — the client receives only the resulting HTML. This makes it ideal for dynamic web content, form processing, session management, and database interaction.\n\nModern PHP (8.0+) has evolved dramatically from its early days. It now features strict typing, named arguments, match expressions, enums, fibers (async), and attributes. Frameworks like Laravel and Symfony have established PHP as a professional enterprise language.`, keyPoints: ['Powers ~77% of all websites (WordPress, Wikipedia).', 'Server-side execution — client receives only HTML.', 'Modern PHP 8+ is a professional, typed language.', 'Laravel is the most popular modern PHP framework.'] },
      { title: 'Installation & Setup', content: `PHP can be installed standalone or as part of a stack. XAMPP (Windows/macOS/Linux) bundles Apache, MySQL, and PHP together — ideal for beginners.\n\nFor production-like environments, install PHP-FPM (FastCGI Process Manager) with Nginx. Docker provides the cleanest setup: run php:8.2-fpm as a container.\n\nComposer is PHP's dependency manager (similar to npm for Node.js). It manages packages from Packagist (packagist.org) and handles autoloading.`, code: `# Install PHP (Ubuntu)\nsudo apt install php php-cli php-fpm php-mysql php-mbstring\n\n# Verify\nphp -v\n\n# Built-in development server\nphp -S localhost:8000\n\n# Install Composer\ncurl -sS https://getcomposer.org/installer | php\nsudo mv composer.phar /usr/local/bin/composer\n\n# Create Laravel project\ncomposer create-project laravel/laravel my-app\ncd my-app\nphp artisan serve`, codeLabel: 'Installation Commands', keyPoints: ['XAMPP for quick local setup, Docker for production.', 'Composer is the standard package manager.', 'php -S localhost:8000 for built-in dev server.', 'Laravel: composer create-project laravel/laravel app.'] },
      { title: 'Core Syntax & Features', content: `PHP variables start with $ (e.g., $name = "Alice"). It is dynamically typed but supports strict type declarations since PHP 7. Use declare(strict_types=1) at the top of files to enforce type checking.\n\nPHP 8 introduced powerful features: Named arguments (improved readability), Match expressions (safer than switch), Enums (type-safe constants), and Attributes (metadata annotations like Java's annotations).\n\nPHP excels at database interaction through PDO (PHP Data Objects), which provides a unified interface for multiple databases with prepared statements for SQL injection prevention.`, code: `<?php\ndeclare(strict_types=1);\n\n// Modern PHP\nfunction greet(string $name, int $age): string {\n    return match(true) {\n        $age < 18 => "Hi $name, you're young!",\n        $age < 65 => "Hello $name!",\n        default   => "Respected $name!"\n    };\n}\n\n// Enum (PHP 8.1+)\nenum Status: string {\n    case Active = 'active';\n    case Inactive = 'inactive';\n    case Banned = 'banned';\n}\n\n// PDO Database query (prepared statement)\n$pdo = new PDO('mysql:host=localhost;dbname=myapp', 'root', '');\n$stmt = $pdo->prepare('SELECT * FROM users WHERE email = :email');\n$stmt->execute(['email' => $email]);\n$user = $stmt->fetch(PDO::FETCH_ASSOC);`, codeLabel: 'Modern PHP 8+', keyPoints: ['declare(strict_types=1) for type safety.', 'Match expressions are safer than switch statements.', 'Enums provide type-safe constants (PHP 8.1+).', 'Always use PDO prepared statements for database queries.'] },
      { title: 'Laravel Framework', content: `Laravel is the most popular PHP framework, known for its elegant syntax, powerful ORM (Eloquent), and extensive ecosystem. It follows the MVC (Model-View-Controller) pattern.\n\nKey Laravel components: Eloquent ORM (database modeling with elegant syntax), Blade Templates (server-side templating engine), Artisan CLI (code generation and task running), Migrations (versioned database schema changes), and Middleware (request/response filtering).\n\nLaravel's ecosystem includes: Livewire (reactive UI without JavaScript), Inertia.js (SPA bridge with Vue/React), Laravel Forge (server provisioning), and Laravel Vapor (serverless deployment on AWS Lambda).`, code: `// Route (routes/web.php)\nRoute::get('/users', [UserController::class, 'index']);\nRoute::post('/users', [UserController::class, 'store']);\n\n// Controller\nclass UserController extends Controller {\n    public function index() {\n        $users = User::where('active', true)\n                     ->orderBy('name')\n                     ->paginate(15);\n        return view('users.index', compact('users'));\n    }\n\n    public function store(Request $request) {\n        $validated = $request->validate([\n            'name' => 'required|max:255',\n            'email' => 'required|email|unique:users',\n        ]);\n        User::create($validated);\n        return redirect('/users');\n    }\n}\n\n// Eloquent Model\nclass User extends Model {\n    protected $fillable = ['name', 'email'];\n    public function posts() {\n        return $this->hasMany(Post::class);\n    }\n}\n\n# Artisan commands\nphp artisan make:model Post -mc  # Model + Migration + Controller\nphp artisan migrate\nphp artisan serve`, codeLabel: 'Laravel Example', keyPoints: ['Eloquent ORM provides elegant database interaction.', 'Artisan CLI generates boilerplate code.', 'Blade templates for server-side rendering.', 'Migrations version your database schema.'] },
      { title: 'Build & Deploy', content: `PHP applications don't require compilation. For deployment, configure Nginx + PHP-FPM for production performance. Use Composer install --no-dev to exclude development dependencies.\n\nLaravel deployment checklist: Run composer install --no-dev, set APP_ENV=production, run php artisan config:cache, php artisan route:cache, and php artisan view:cache for optimization.\n\nDocker or managed hosting (Laravel Forge, Ploi) simplifies deployment. For serverless, Laravel Vapor deploys to AWS Lambda.`, code: `# Production deployment\ncomposer install --optimize-autoloader --no-dev\nphp artisan config:cache\nphp artisan route:cache\nphp artisan view:cache\n\n# Nginx config\nserver {\n    listen 80;\n    root /var/www/html/public;\n    index index.php;\n\n    location / {\n        try_files $uri $uri/ /index.php?$query_string;\n    }\n\n    location ~ \\.php$ {\n        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;\n        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;\n        include fastcgi_params;\n    }\n}`, codeLabel: 'Deployment Config', keyPoints: ['Nginx + PHP-FPM for production performance.', 'Cache config, routes, and views for speed.', 'Use --no-dev flag in production.', 'Laravel Forge or Docker for deployment automation.'] }
    ]
  },
  {
    id: 'sql', title: 'SQL Database', description: 'Relational database management with structured queries.',
    officialDocs: 'https://www.postgresql.org/docs/', tutorialLink: 'https://www.w3schools.com/sql/', exerciseLink: 'https://www.w3schools.com/sql/sql_exercises.asp',
    sections: [
      { title: 'What is SQL', content: `SQL (Structured Query Language) is the standard language for managing and manipulating relational databases. Relational databases store data in structured tables with rows (records) and columns (fields), linked by relationships (foreign keys).\n\nSQL is declarative — you specify what data you want, not how to get it. The database engine optimizes the execution plan. Major relational databases include PostgreSQL (most feature-rich open source), MySQL/MariaDB (most popular), SQL Server (Microsoft), Oracle, and SQLite (embedded/local).\n\nSQL is divided into sublanguages: DDL (Data Definition Language — CREATE, ALTER, DROP for schema), DML (Data Manipulation Language — SELECT, INSERT, UPDATE, DELETE for data), DCL (Data Control Language — GRANT, REVOKE for permissions), and TCL (Transaction Control — COMMIT, ROLLBACK).`, keyPoints: ['SQL is declarative — specify what, not how.', 'Tables store data in rows and columns.', 'PostgreSQL is the most feature-rich open source RDBMS.', 'DDL for schema, DML for data, DCL for permissions.'] },
      { title: 'Tables, Schema & Data Types', content: `A table is the fundamental storage unit in SQL. Each table has columns (with specific data types) and rows (each row is a record). The schema is the overall design — all tables, relationships, constraints, and indexes.\n\nCommon data types: INTEGER/BIGINT (whole numbers), DECIMAL/NUMERIC (exact precision), VARCHAR(n) (variable-length string), TEXT (unlimited string), BOOLEAN, DATE, TIMESTAMP, UUID, JSON/JSONB (PostgreSQL).\n\nConstraints enforce data integrity: PRIMARY KEY (unique identifier), FOREIGN KEY (relationships between tables), NOT NULL (required field), UNIQUE (no duplicates), CHECK (custom validation), DEFAULT (fallback value).`, code: `-- Create tables with relationships\nCREATE TABLE users (\n    id SERIAL PRIMARY KEY,\n    name VARCHAR(100) NOT NULL,\n    email VARCHAR(255) UNIQUE NOT NULL,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE TABLE posts (\n    id SERIAL PRIMARY KEY,\n    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE,\n    title VARCHAR(200) NOT NULL,\n    body TEXT,\n    published BOOLEAN DEFAULT false,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Add index for performance\nCREATE INDEX idx_posts_user ON posts(user_id);\n\n-- Modify table\nALTER TABLE users ADD COLUMN role VARCHAR(20) DEFAULT 'user';`, codeLabel: 'Table Creation', keyPoints: ['PRIMARY KEY uniquely identifies each row.', 'FOREIGN KEY creates relationships between tables.', 'Always add indexes on columns used in WHERE/JOIN.', 'ON DELETE CASCADE automatically removes related records.'] },
      { title: 'CRUD Operations', content: `CRUD represents the four basic database operations: Create (INSERT), Read (SELECT), Update (UPDATE), Delete (DELETE). These form the foundation of all data manipulation.\n\nSELECT is the most complex and powerful operation. It supports filtering (WHERE), sorting (ORDER BY), limiting results (LIMIT/OFFSET for pagination), aggregation (COUNT, SUM, AVG, MIN, MAX), grouping (GROUP BY), and filtering groups (HAVING).\n\nAlways use parameterized queries (prepared statements) when incorporating user input to prevent SQL injection attacks.`, code: `-- INSERT\nINSERT INTO users (name, email) VALUES ('Alice', 'alice@mail.com');\nINSERT INTO users (name, email) VALUES\n  ('Bob', 'bob@mail.com'),\n  ('Carol', 'carol@mail.com');\n\n-- SELECT with filtering and sorting\nSELECT name, email FROM users\nWHERE created_at > '2026-01-01'\nORDER BY name ASC\nLIMIT 10 OFFSET 0;\n\n-- Aggregation\nSELECT role, COUNT(*) as user_count\nFROM users\nGROUP BY role\nHAVING COUNT(*) > 5;\n\n-- UPDATE\nUPDATE users SET role = 'admin' WHERE email = 'alice@mail.com';\n\n-- DELETE\nDELETE FROM posts WHERE published = false AND created_at < '2025-01-01';`, codeLabel: 'CRUD Operations', keyPoints: ['SELECT is the most powerful and complex operation.', 'WHERE filters rows, HAVING filters groups.', 'LIMIT/OFFSET for pagination.', 'Always use prepared statements to prevent SQL injection.'] },
      { title: 'JOINs & Relationships', content: `JOINs combine rows from multiple tables based on related columns. They are fundamental to relational databases and enable complex queries across related data.\n\nINNER JOIN returns only rows that have matches in both tables. LEFT JOIN returns ALL rows from the left table and matching rows from the right (NULL if no match). RIGHT JOIN is the opposite. FULL OUTER JOIN returns all rows from both tables.\n\nSelf-joins join a table to itself (useful for hierarchical data like employees/managers). Cross joins produce a Cartesian product (every row combined with every other row).`, code: `-- INNER JOIN: Users with their posts\nSELECT u.name, p.title, p.created_at\nFROM users u\nINNER JOIN posts p ON u.id = p.user_id\nWHERE p.published = true\nORDER BY p.created_at DESC;\n\n-- LEFT JOIN: All users, even without posts\nSELECT u.name, COUNT(p.id) as post_count\nFROM users u\nLEFT JOIN posts p ON u.id = p.user_id\nGROUP BY u.name\nORDER BY post_count DESC;\n\n-- Multiple JOINs\nSELECT u.name, p.title, c.body as comment\nFROM users u\nJOIN posts p ON u.id = p.user_id\nJOIN comments c ON p.id = c.post_id\nWHERE u.role = 'admin';\n\n-- Subquery\nSELECT name FROM users\nWHERE id IN (SELECT user_id FROM posts GROUP BY user_id HAVING COUNT(*) > 5);`, codeLabel: 'JOIN Examples', keyPoints: ['INNER JOIN: only matching rows from both tables.', 'LEFT JOIN: all rows from left, matches from right.', 'Always use table aliases (u, p) for readability.', 'Prefer JOINs over subqueries for performance.'] },
      { title: 'Indexes & Performance', content: `Indexes dramatically speed up queries by creating sorted lookup structures (typically B-trees) on specific columns. Without indexes, the database must scan every row in the table (full table scan).\n\nCreate indexes on columns frequently used in WHERE clauses, JOIN conditions, and ORDER BY. Primary keys and UNIQUE constraints automatically create indexes.\n\nHowever, indexes slow down writes (INSERT, UPDATE, DELETE) because the index must be updated. They also consume disk space. Don't index everything — only columns that significantly benefit read performance.\n\nThe EXPLAIN command shows the query execution plan — which indexes are used, estimated row counts, and scan types. Use EXPLAIN ANALYZE for actual execution statistics.`, code: `-- Create indexes\nCREATE INDEX idx_users_email ON users(email);\nCREATE INDEX idx_posts_created ON posts(created_at DESC);\n\n-- Composite index (for queries filtering on both columns)\nCREATE INDEX idx_posts_user_pub ON posts(user_id, published);\n\n-- Analyze query performance\nEXPLAIN ANALYZE\nSELECT * FROM posts WHERE user_id = 42 AND published = true;\n\n-- Output shows:\n-- Index Scan using idx_posts_user_pub on posts\n-- Execution Time: 0.045 ms (fast!)\n\n-- vs without index:\n-- Seq Scan on posts\n-- Execution Time: 125.3 ms (slow!)`, codeLabel: 'Index & Performance', keyPoints: ['Indexes speed up reads but slow down writes.', 'Index columns used in WHERE, JOIN, and ORDER BY.', 'EXPLAIN ANALYZE shows actual query performance.', 'Composite indexes optimize multi-column queries.'] },
      { title: 'Database Design & Normalization', content: `Good database design prevents data redundancy, anomalies, and inconsistencies. Normalization is the process of organizing tables to minimize duplication.\n\n1NF (First Normal Form): Eliminate repeating groups — each cell contains a single value, not lists. 2NF: Remove partial dependencies — non-key columns must depend on the entire primary key. 3NF: Remove transitive dependencies — non-key columns must depend directly on the primary key, not on other non-key columns.\n\nIn practice, most applications target 3NF. Denormalization (intentionally adding redundancy) is sometimes done for read-heavy workloads where JOIN performance becomes a bottleneck.\n\nDesign principles: Use UUID or auto-incrementing integers for primary keys. Define foreign key constraints with appropriate ON DELETE behavior. Add created_at and updated_at timestamps to every table. Use migration tools to version schema changes.`, keyPoints: ['Normalization reduces data redundancy and anomalies.', 'Most apps should target 3NF (Third Normal Form).', 'Denormalize only when JOIN performance is a proven bottleneck.', 'Use migrations to version schema changes over time.'] }
    ]
  },
  {
    id: 'nosql', title: 'NoSQL Database', description: 'Flexible, non-relational databases for modern applications.',
    officialDocs: 'https://www.mongodb.com/docs/', tutorialLink: 'https://www.w3schools.com/mongodb/', exerciseLink: null,
    sections: [
      { title: 'What is NoSQL', content: `NoSQL (Not Only SQL) databases store data in formats other than traditional relational tables. They are designed for specific use cases where relational databases struggle: massive scale, flexible schemas, high write throughput, and distributed systems.\n\nFour main types: Document stores (MongoDB, Firestore — JSON-like documents), Key-Value stores (Redis, DynamoDB — simple key-value pairs, extremely fast), Column-Family stores (Cassandra, HBase — columns grouped into families, great for time-series), and Graph databases (Neo4j — nodes and edges for complex relationships).\n\nNoSQL databases typically follow BASE (Basically Available, Soft state, Eventually consistent) rather than ACID. They sacrifice strict consistency for availability and partition tolerance (the CAP theorem).`, keyPoints: ['4 types: Document, Key-Value, Column-Family, Graph.', 'Designed for scale, flexibility, and distributed systems.', 'BASE model: eventually consistent, highly available.', 'Choose based on data access patterns, not popularity.'] },
      { title: 'MongoDB CRUD Operations', content: `MongoDB is the most popular document database. It stores data as BSON (Binary JSON) documents — flexible, nested structures without predefined schemas. Documents are stored in Collections (analogous to tables).\n\nMongoDB operations are designed to be intuitive and closely map to how applications use data. Documents can contain nested objects and arrays, allowing you to store related data together instead of normalizing across tables.`, code: `// Insert documents\ndb.users.insertOne({\n  name: "Alice",\n  email: "alice@mail.com",\n  age: 28,\n  address: { city: "NYC", zip: "10001" },\n  hobbies: ["reading", "coding", "hiking"]\n});\n\n// Find with query operators\ndb.users.find({\n  age: { $gte: 25, $lte: 35 },\n  "address.city": "NYC"\n}).sort({ name: 1 }).limit(10);\n\n// Update\ndb.users.updateOne(\n  { email: "alice@mail.com" },\n  { $set: { age: 29 }, $push: { hobbies: "gaming" } }\n);\n\n// Aggregation pipeline\ndb.orders.aggregate([\n  { $match: { status: "completed" } },\n  { $group: { _id: "$customerId", total: { $sum: "$amount" } } },\n  { $sort: { total: -1 } },\n  { $limit: 10 }\n]);`, codeLabel: 'MongoDB Operations', keyPoints: ['Documents are flexible BSON (JSON-like) structures.', 'Query operators: $gte, $lte, $in, $regex, $exists.', 'Aggregation pipeline for complex data transformations.', 'Embed related data to avoid the need for JOINs.'] },
      { title: 'Schema Design Patterns', content: `MongoDB schema design is fundamentally different from SQL. Instead of normalizing data across tables, you design based on how your application queries data.\n\nEmbed (denormalize) when data is frequently accessed together, there is a 1:1 or 1:few relationship, and the embedded data doesn't grow without bound. Example: user addresses, order line items.\n\nReference (normalize) when data is accessed independently, there is a 1:many or many:many relationship, or the related data is large/grows unboundedly. Example: user's posts (reference), post comments (embed if few, reference if many).\n\nAnti-pattern: unbounded arrays. If a document's array can grow indefinitely, it will eventually exceed MongoDB's 16MB document size limit. Move such data to a separate collection with references.`, keyPoints: ['Design for query patterns, not data relationships.', 'Embed data accessed together (1:1, 1:few).', 'Reference data accessed independently (1:many).', 'Avoid unbounded arrays (16MB document limit).'] },
      { title: 'MongoDB vs SQL — When to Use Which', content: `Use SQL (relational) when: Data is highly structured with clear relationships. You need complex JOIN queries. ACID transactions are critical (banking, inventory). Data integrity and consistency are top priorities.\n\nUse MongoDB when: Schema needs to evolve rapidly (startups, prototyping). Data is naturally document-shaped (user profiles, product catalogs). You need horizontal scaling across distributed servers. Read/write patterns are simple (CRUD per document).\n\nUse Redis (Key-Value) when: You need sub-millisecond response times. Use cases include caching, session storage, rate limiting, and real-time leaderboards.\n\nUse Neo4j (Graph) when: Relationships are the primary concern — social networks, recommendation engines, fraud detection, knowledge graphs.\n\nMany modern applications use polyglot persistence — multiple databases, each optimized for specific use cases. Example: PostgreSQL for transactional data, MongoDB for product catalogs, Redis for caching, Elasticsearch for search.`, keyPoints: ['SQL for structured data with complex relationships.', 'MongoDB for flexible schemas and horizontal scaling.', 'Redis for caching and sub-millisecond reads.', 'Polyglot persistence: use the right DB for each use case.'] }
    ]
  },
  {
    id: 'docker', title: 'Docker', description: 'Containerization platform for consistent, portable deployments.',
    officialDocs: 'https://docs.docker.com/', tutorialLink: 'https://docs.docker.com/get-started/', exerciseLink: null,
    sections: [
      { title: 'What is Docker', content: `Docker is a platform for developing, shipping, and running applications inside containers. Containers are lightweight, isolated environments that package an application with everything it needs to run — code, runtime, libraries, and system tools.\n\nUnlike virtual machines (which emulate entire operating systems), containers share the host OS kernel and only isolate the application layer. This makes them extremely lightweight (MBs vs GBs), fast to start (seconds vs minutes), and efficient with resources.\n\nDocker solves the "works on my machine" problem. A containerized application runs identically on a developer's laptop, a CI/CD server, a staging environment, and production. This consistency is Docker's core value proposition.`, keyPoints: ['Containers package apps with all dependencies.', 'Lightweight and fast compared to virtual machines.', 'Eliminates "works on my machine" problems.', 'Applications run identically everywhere.'] },
      { title: 'Installation & Architecture', content: `Docker Desktop (Windows/macOS) bundles the Docker Engine, CLI, and Docker Compose. On Linux, install Docker Engine directly. Verify installation with docker --version.\n\nDocker architecture: The Docker Daemon (background service) manages containers, images, networks, and volumes. The Docker CLI sends commands to the daemon via its REST API. Docker Hub (hub.docker.com) is the default image registry — a cloud repository of pre-built images.`, code: `# Verify installation\ndocker --version\ndocker compose version\n\n# Test with Hello World\ndocker run hello-world\n\n# Run Nginx web server\ndocker run -d -p 8080:80 --name web nginx\n\n# List running containers\ndocker ps\n\n# Stop and remove\ndocker stop web\ndocker rm web\n\n# List all images\ndocker images`, codeLabel: 'Docker Basics', keyPoints: ['Docker Desktop for Windows/macOS, Docker Engine for Linux.', 'Daemon manages containers, CLI sends commands.', 'Docker Hub is the default image registry.', 'docker run starts containers, docker ps lists them.'] },
      { title: 'Dockerfile Deep Dive', content: `A Dockerfile is a text file with instructions for building a Docker image. Each instruction creates a layer in the image. Docker caches layers — if a layer hasn't changed, Docker reuses the cache, dramatically speeding up builds.\n\nKey instructions: FROM (base image), WORKDIR (set working directory), COPY (copy files from host), RUN (execute commands during build), ENV (set environment variables), EXPOSE (document which port the app uses), CMD (default command when container starts).\n\nOptimization: Order instructions from least-frequently-changed to most-frequently-changed. Copy package.json and install dependencies BEFORE copying source code — this way, dependency installation is cached unless package.json changes.`, code: `# Optimized Node.js Dockerfile\nFROM node:20-alpine\n\n# Set working directory\nWORKDIR /app\n\n# Copy dependency files first (caching layer)\nCOPY package.json package-lock.json ./\n\n# Install dependencies (cached if package.json unchanged)\nRUN npm ci --only=production\n\n# Copy source code (changes most frequently)\nCOPY . .\n\n# Set environment\nENV NODE_ENV=production\nENV PORT=3000\n\n# Document which port is used\nEXPOSE 3000\n\n# Start command\nCMD ["node", "server.js"]\n\n# Build the image\n# docker build -t my-app:1.0 .\n# docker run -d -p 3000:3000 my-app:1.0`, codeLabel: 'Optimized Dockerfile', keyPoints: ['Each instruction creates a cached layer.', 'Copy dependencies before source code for better caching.', 'Use alpine-based images for smaller size.', 'CMD defines the default startup command.'] },
      { title: 'Docker Compose', content: `Docker Compose defines and runs multi-container applications. Instead of running multiple docker run commands, you define all services in a docker-compose.yml file and start everything with docker compose up.\n\nServices are containers defined in the compose file. They can communicate over a shared network using service names as hostnames (e.g., db:5432 instead of localhost:5432). Volumes persist data between container restarts. Environment variables configure services.`, code: `# docker-compose.yml\nversion: "3.8"\nservices:\n  app:\n    build: .\n    ports:\n      - "3000:3000"\n    environment:\n      - DATABASE_URL=postgres://user:pass@db:5432/mydb\n      - REDIS_URL=redis://cache:6379\n    depends_on:\n      - db\n      - cache\n\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: user\n      POSTGRES_PASSWORD: pass\n      POSTGRES_DB: mydb\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    ports:\n      - "5432:5432"\n\n  cache:\n    image: redis:7-alpine\n    ports:\n      - "6379:6379"\n\nvolumes:\n  pgdata:\n\n# Commands\n# docker compose up -d       # Start all services\n# docker compose down        # Stop all services\n# docker compose logs -f app # Follow app logs`, codeLabel: 'Docker Compose', keyPoints: ['Define multi-container apps in one YAML file.', 'Services communicate via service names (db, cache).', 'Volumes persist data across container restarts.', 'depends_on controls startup order.'] },
      { title: 'Docker Best Practices', content: `Use specific image tags (node:20-alpine, not node:latest) for reproducible builds. Use .dockerignore to exclude node_modules, .git, and other unnecessary files from the build context.\n\nMulti-stage builds separate the build environment from the production image. The build stage installs dev dependencies and compiles code. The production stage copies only the compiled output, resulting in a much smaller image.\n\nSecurity: Don't run containers as root (USER node). Don't store secrets in Dockerfiles or images — use environment variables or Docker secrets. Scan images for vulnerabilities with docker scout.\n\nKeep images small: Use alpine-based images, minimize layers (combine RUN commands), and remove cache files after installing packages.`, code: `# Multi-stage build (React app)\nFROM node:20-alpine AS build\nWORKDIR /app\nCOPY package*.json .\nRUN npm ci\nCOPY . .\nRUN npm run build\n\n# Production stage — only compiled output\nFROM nginx:alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\n\n# .dockerignore\nnode_modules\n.git\n*.md\n.env\ndist`, codeLabel: 'Multi-Stage Build', keyPoints: ['Use specific image tags, never :latest.', 'Multi-stage builds dramatically reduce image size.', 'Never run containers as root in production.', 'Use .dockerignore to exclude unnecessary files.'] }
    ]
  }
];
