export const cssCourse = {
  id: 'css',
  title: 'CSS',
  description: 'Cascading Style Sheets — styling and layout for the web.',
  officialDocs: 'https://developer.mozilla.org/en-US/docs/Web/CSS',
  tutorialLink: 'https://www.w3schools.com/css/',
  exerciseLink: 'https://www.w3schools.com/css/css_exercises.asp',
  sections: [
    { title: 'What is CSS', content: `CSS (Cascading Style Sheets) controls how HTML elements are visually presented. While HTML defines the structure and content of a page, CSS defines its appearance — colors, fonts, spacing, layout, animations, and responsive behavior.\n\nThe word "Cascading" refers to the priority system CSS uses when multiple rules target the same element. Rules cascade based on specificity, source order, and importance. Understanding this cascade is fundamental to writing predictable, maintainable styles.\n\nCSS can be applied in three ways: Inline (style attribute on an element), Internal (a <style> tag in the <head>), and External (a linked .css file). External stylesheets are strongly preferred because they separate concerns, are cached by browsers, and can be reused across pages.\n\nModern CSS is remarkably powerful. Features like Flexbox, Grid, custom properties (variables), container queries, and advanced selectors have eliminated the need for many JavaScript-based layout solutions. Learning CSS deeply will make you a significantly faster and more effective frontend developer.`, image: 'https://images.unsplash.com/photo-1507721999472-8ed4421c4af2?w=800&auto=format&fit=crop&q=60', keyPoints: ['CSS controls presentation; HTML controls structure.', '"Cascading" refers to the priority/specificity system.', 'Use external stylesheets for maintainability and caching.', 'Modern CSS (Flexbox, Grid, variables) is extremely powerful.'] },
    { title: 'Selectors & Specificity', content: `CSS selectors determine which HTML elements a rule applies to. Mastering selectors is essential because they are the foundation of every style rule you write.\n\nBasic selectors: Element selector (p, h1, div) targets all elements of that type. Class selector (.card, .active) targets elements with a specific class attribute. ID selector (#main-nav) targets the single element with that ID. Universal selector (*) targets everything.\n\nCombinator selectors: Descendant (div p) selects all <p> inside any <div>. Child (div > p) selects only direct children. Adjacent sibling (h2 + p) selects the first <p> immediately after an <h2>. General sibling (h2 ~ p) selects all <p> siblings after an <h2>.\n\nPseudo-classes target elements in a specific state: :hover (mouse over), :focus (keyboard focus), :first-child, :nth-child(2n) (every even element), :not(.hidden). Pseudo-elements style specific parts: ::before and ::after insert content, ::first-line styles the first line of text.\n\nSpecificity is how browsers decide which rule wins when multiple rules target the same element. The hierarchy: Inline styles (1000) > ID selectors (100) > Class/pseudo-class selectors (10) > Element selectors (1). !important overrides everything but should be avoided.`, code: `/* Element, Class, ID */\np { color: gray; }\n.highlight { background: yellow; }\n#hero { font-size: 3rem; }\n\n/* Combinators */\nnav > a { font-weight: bold; }       /* Direct children only */\nh2 + p { margin-top: 0; }            /* Adjacent sibling */\n.card:hover { transform: scale(1.02); } /* Pseudo-class */\n\n/* Pseudo-elements */\n.quote::before { content: "\\201C"; }  /* Left double quote */\n\n/* Attribute selectors */\ninput[type="email"] { border-color: blue; }\na[href^="https"] { color: green; }     /* Starts with https */\n\n/* :nth-child */\ntr:nth-child(even) { background: #f5f5f5; }`, codeLabel: 'Selector Examples', keyPoints: ['Specificity: inline > ID > class > element.', 'Avoid !important — it makes debugging painful.', 'Use class selectors (.name) for almost everything.', 'Pseudo-classes (:hover, :focus) add interactivity.'] },
    { title: 'The Box Model', content: `Every HTML element is a rectangular box. The CSS Box Model defines the space each element occupies. From inside out, the box has four layers: Content (the actual text/image), Padding (space between content and border), Border (the edge of the box), and Margin (space between this box and neighboring elements).\n\nBy default, width and height apply only to the content area. Padding and border are added on top, making the total size larger than expected. This is "content-box" sizing and is confusing. The fix is to use box-sizing: border-box, which makes width and height include padding and border. Most modern CSS resets apply this globally.\n\nMargin has a unique behavior called "margin collapsing." When two vertical margins touch, they collapse into one (the larger one wins). This only happens vertically, and understanding it prevents many frustrating spacing bugs.\n\nPadding cannot be negative, but margin can. Negative margins pull elements closer or even overlap them. The display property fundamentally changes how the box model applies: block elements take full width, inline elements only take the width of their content, and inline-block combines both behaviors.`, code: `/* Apply border-box globally */\n*, *::before, *::after {\n  box-sizing: border-box;\n}\n\n.card {\n  width: 300px;        /* Total width = 300px (with border-box) */\n  padding: 20px;       /* Inside space */\n  border: 2px solid #e2e8f0;\n  margin: 16px;        /* Outside space */\n  border-radius: 12px;\n}\n\n/* Margin collapse example: these <p> gaps don't add up */\np { margin: 20px 0; }  /* Gap between paragraphs = 20px, not 40px */`, codeLabel: 'Box Model Example', keyPoints: ['Content → Padding → Border → Margin (inside out).', 'Always use box-sizing: border-box globally.', 'Vertical margins collapse; horizontal margins do not.', 'padding cannot be negative; margin can.'] },
    { title: 'Flexbox Layout', content: `Flexbox (Flexible Box Layout) is designed for one-dimensional layouts — arranging items in a row or a column. It distributes space among items and handles alignment with minimal code. Flexbox solved decades of CSS layout pain (centering, equal-height columns, responsive reordering).\n\nTo use Flexbox, set display: flex on the parent container. This makes all direct children "flex items." By default, items are arranged in a row, shrink to fit, and stretch vertically to match the tallest item.\n\nThe flex-direction property sets the main axis: row (default, horizontal), column (vertical), row-reverse, column-reverse. justify-content aligns items along the main axis: flex-start, center, flex-end, space-between (equal gaps with items at edges), space-around, space-evenly.\n\nalign-items aligns items along the cross axis (perpendicular to main): stretch (default), center, flex-start, flex-end, baseline. flex-wrap determines if items wrap to a new line when they exceed the container width.\n\nOn individual flex items, flex-grow controls how much extra space an item absorbs, flex-shrink controls how much it shrinks, and flex-basis sets the initial size. The shorthand flex: 1 means "grow equally, shrink equally, basis auto." The gap property adds consistent spacing between items.`, code: `/* Center anything vertically and horizontally */\n.center-everything {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  min-height: 100vh;\n}\n\n/* Navigation bar */\n.navbar {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 1rem 2rem;\n}\n\n/* Card grid that wraps */\n.card-container {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 1.5rem;\n}\n.card-container .card {\n  flex: 1 1 300px; /* Grow, shrink, min 300px */\n}`, codeLabel: 'Flexbox Layout Patterns', keyPoints: ['display: flex on parent makes children flex items.', 'justify-content: main axis; align-items: cross axis.', 'flex: 1 makes items share space equally.', 'Use gap for consistent spacing between items.'] },
    { title: 'CSS Grid Layout', content: `CSS Grid is designed for two-dimensional layouts — controlling both rows and columns simultaneously. While Flexbox handles one direction at a time, Grid handles the entire page layout in a single declaration.\n\nSet display: grid on the parent. Define columns with grid-template-columns and rows with grid-template-rows. The fr (fraction) unit distributes available space proportionally: grid-template-columns: 1fr 2fr means the second column is twice as wide as the first.\n\nThe repeat() function creates multiple tracks: repeat(3, 1fr) creates three equal columns. For responsive grids, use repeat(auto-fit, minmax(280px, 1fr)) — this creates as many columns as will fit, each at least 280px wide, and they grow to fill remaining space. No media queries needed.\n\ngap (or row-gap and column-gap) adds spacing between grid cells. grid-template-areas lets you name regions and arrange them visually in your CSS, creating highly readable layout declarations.\n\nGrid items can span multiple cells using grid-column: span 2 or be placed explicitly using grid-column: 1 / 3 (start at line 1, end at line 3). The order property reorders items visually without changing HTML source order.`, code: `/* Responsive auto-fit grid (no media queries!) */\n.grid-auto {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));\n  gap: 1.5rem;\n}\n\n/* Named areas layout */\n.page-layout {\n  display: grid;\n  grid-template-areas:\n    "header  header"\n    "sidebar main"\n    "footer  footer";\n  grid-template-columns: 250px 1fr;\n  grid-template-rows: auto 1fr auto;\n  min-height: 100vh;\n}\n.page-header  { grid-area: header; }\n.page-sidebar { grid-area: sidebar; }\n.page-main    { grid-area: main; }\n.page-footer  { grid-area: footer; }`, codeLabel: 'CSS Grid Patterns', keyPoints: ['Grid handles 2D layouts (rows AND columns).', 'fr unit distributes space proportionally.', 'auto-fit + minmax() creates responsive grids without media queries.', 'grid-template-areas creates visual, readable layouts.'] },
    { title: 'Responsive Design & Media Queries', content: `Responsive design ensures your website looks and works well on all screen sizes — from small phones (320px) to large monitors (2560px+). It is not optional in modern web development.\n\nThe foundation is the viewport meta tag in HTML: <meta name="viewport" content="width=device-width, initial-scale=1.0">. Without this, mobile browsers render the page at desktop width and scale it down.\n\nMedia queries apply styles conditionally based on device characteristics. The most common pattern is min-width (mobile-first): start with styles for small screens, then add rules for larger screens. This approach is preferred because mobile styles are simpler and serve as the baseline.\n\nBeyond media queries, use relative units for responsive sizing: rem for font sizes (relative to root font size), % for widths, vh/vw for viewport-relative dimensions. Avoid fixed pixel widths on containers. Use max-width instead of width to allow elements to shrink on small screens.\n\nFlexbox and Grid with auto-fit/minmax create responsive layouts without any media queries at all. The clamp() function provides fluid typography: font-size: clamp(1rem, 2.5vw, 2rem) — the font smoothly scales between 1rem and 2rem based on viewport width.`, code: `/* Mobile-first approach */\n.container {\n  padding: 1rem;\n  max-width: 100%;\n}\n\n/* Tablet (768px and up) */\n@media (min-width: 768px) {\n  .container { padding: 2rem; max-width: 768px; margin: 0 auto; }\n  .grid { grid-template-columns: repeat(2, 1fr); }\n}\n\n/* Desktop (1024px and up) */\n@media (min-width: 1024px) {\n  .container { padding: 3rem; max-width: 1200px; }\n  .grid { grid-template-columns: repeat(3, 1fr); }\n}\n\n/* Fluid typography — no media queries needed */\nh1 { font-size: clamp(1.5rem, 4vw, 3rem); }\np  { font-size: clamp(0.875rem, 1.5vw, 1.125rem); }`, codeLabel: 'Responsive Design Patterns', keyPoints: ['Always use the viewport meta tag for mobile.', 'Mobile-first (min-width) is the preferred approach.', 'Use relative units (rem, %, vw) instead of fixed pixels.', 'clamp() enables fluid sizing without media queries.'] },
    { title: 'Colors, Gradients & Backgrounds', content: `CSS offers multiple color formats. Hex (#0047AB), RGB (rgb(0, 71, 171)), and HSL (hsl(215, 100%, 34%)) are the most common. HSL is the most human-readable because you can intuitively adjust hue (color), saturation (vibrancy), and lightness (brightness) independently.\n\nAll three formats support an alpha (transparency) channel: rgba(0,71,171,0.5) or hsla(215,100%,34%,0.5) for 50% opacity. Modern CSS also accepts #0047AB80 (hex with alpha).\n\nLinear gradients create smooth transitions between colors along a line. Radial gradients radiate from a center point. Conic gradients rotate around a center (useful for pie charts). Gradients can be used anywhere a color is accepted — backgrounds, borders, text (with a clip trick).\n\nThe background property is a powerful shorthand that combines color, image, repeat, position, and size. background-size: cover scales the image to cover the entire element (may crop), while contain ensures the full image is visible (may leave empty space).`, code: `/* HSL is most intuitive */\n:root {\n  --primary: hsl(215, 100%, 34%);\n  --primary-hover: hsl(215, 100%, 28%);\n  --primary-light: hsl(215, 100%, 95%);\n}\n\n/* Linear gradient */\n.hero {\n  background: linear-gradient(135deg, #0047AB 0%, #002D6E 100%);\n}\n\n/* Radial gradient overlay on image */\n.card-bg {\n  background:\n    radial-gradient(circle at top right, rgba(0,71,171,0.3), transparent),\n    url('photo.jpg') center/cover no-repeat;\n}\n\n/* Glassmorphism effect */\n.glass {\n  background: rgba(255, 255, 255, 0.15);\n  backdrop-filter: blur(10px);\n  border: 1px solid rgba(255, 255, 255, 0.2);\n}`, codeLabel: 'Color & Background Techniques', keyPoints: ['HSL is the most human-readable color format.', 'Use CSS custom properties for consistent theming.', 'Gradients can be layered with images for rich effects.', 'backdrop-filter creates glassmorphism effects.'] },
    { title: 'Transitions & Animations', content: `CSS transitions animate property changes over time. When a property like color or transform changes (due to :hover, class toggle, etc.), the transition smoothly interpolates between the old and new values instead of snapping instantly.\n\nThe transition property is a shorthand: transition: property duration timing-function delay. Only certain properties can be transitioned — generally those with numeric values (width, height, opacity, color, transform). The "transform" and "opacity" transitions are the most performant because they are handled by the GPU.\n\nTiming functions control the acceleration curve: ease (default, slow start/end), linear (constant speed), ease-in (slow start), ease-out (slow end), ease-in-out (slow both). Custom curves can be defined with cubic-bezier().\n\nCSS @keyframes animations provide full control over multi-step animations. Define named keyframes with percentages (0%, 50%, 100%) and apply them using the animation property. Animations can loop infinitely, alternate direction, and pause/play.\n\nPerformance tip: Only animate transform and opacity. Animating width, height, top, left, or margin triggers expensive layout recalculations. Use transform: translateX() instead of left, and transform: scale() instead of width changes.`, code: `/* Smooth hover transition */\n.button {\n  background: #0047AB;\n  color: white;\n  transform: translateY(0);\n  transition: all 0.3s ease;\n}\n.button:hover {\n  background: #002D6E;\n  transform: translateY(-2px);\n  box-shadow: 0 10px 20px rgba(0,0,0,0.15);\n}\n\n/* Keyframe animation */\n@keyframes fadeInUp {\n  0%   { opacity: 0; transform: translateY(20px); }\n  100% { opacity: 1; transform: translateY(0); }\n}\n.card {\n  animation: fadeInUp 0.5s ease forwards;\n}\n\n/* Infinite pulse */\n@keyframes pulse {\n  0%, 100% { transform: scale(1); }\n  50%      { transform: scale(1.05); }\n}\n.live-dot { animation: pulse 2s ease-in-out infinite; }`, codeLabel: 'Animation Examples', keyPoints: ['Transitions animate between two states smoothly.', 'Only animate transform and opacity for best performance.', '@keyframes allow multi-step, looping animations.', 'Use cubic-bezier() for custom easing curves.'] },
    { title: 'CSS Variables (Custom Properties)', content: `CSS Custom Properties (variables) allow you to define reusable values that can be referenced throughout your stylesheet. They are declared with double-dash prefix (--variable-name) and accessed using the var() function.\n\nVariables are defined on elements and inherited by their descendants. Defining them on :root (the <html> element) makes them globally available. This is the foundation of a design token system — define colors, spacing, fonts, and shadows in one place, reference them everywhere.\n\nUnlike preprocessor variables (SASS $variables), CSS variables are live in the browser. They can be modified at runtime using JavaScript (element.style.setProperty('--color', 'red')) or overridden for specific components. This enables powerful features like theme switching (dark/light mode) without any JavaScript framework.\n\nCSS variables also accept fallback values: var(--primary, #0047AB) uses #0047AB if --primary is not defined. They can reference other variables and be used in calc() expressions for mathematical operations.`, code: `:root {\n  /* Color tokens */\n  --primary: #0047AB;\n  --primary-hover: #003580;\n  --bg-main: #ffffff;\n  --text-primary: #1e293b;\n\n  /* Spacing scale */\n  --space-sm: 0.5rem;\n  --space-md: 1rem;\n  --space-lg: 2rem;\n\n  /* Typography */\n  --font-main: 'Inter', sans-serif;\n  --font-mono: 'JetBrains Mono', monospace;\n}\n\n/* Dark mode override */\n@media (prefers-color-scheme: dark) {\n  :root {\n    --bg-main: #0f172a;\n    --text-primary: #f1f5f9;\n  }\n}\n\n/* Usage */\n.card {\n  background: var(--bg-main);\n  color: var(--text-primary);\n  padding: var(--space-lg);\n  font-family: var(--font-main);\n}`, codeLabel: 'Design Token System with CSS Variables', keyPoints: ['Define on :root for global availability.', 'Live in the browser — can be changed with JS.', 'Foundation for theme switching (dark/light mode).', 'Use var(--name, fallback) for safe defaults.'] },
    { title: 'CSS Best Practices', content: `Consistent, well-organized CSS is essential for maintaining large projects. Without conventions, stylesheets quickly become unmaintainable "spaghetti CSS" where nobody knows what rules affect what elements.\n\nUse a naming methodology. BEM (Block__Element--Modifier) is popular: .card, .card__title, .card__button--disabled. This creates clear, predictable, collision-free class names. Utility-first frameworks like Tailwind take a different approach by using many small, single-purpose classes.\n\nOrganize your CSS logically: reset/normalize first, then design tokens (variables), global styles (body, typography), layout components, then specific UI components. Use comments and separate sections clearly.\n\nAvoid deep nesting and overly specific selectors. A rule like div.container > ul.list > li.item > a.link is fragile — any HTML change breaks it. Prefer flat, class-based selectors: .nav-link is specific enough and resilient.\n\nPrefer mobile-first responsive design. Write base styles for small screens, then use min-width media queries to enhance for larger screens. Use modern layout (Flexbox, Grid) instead of floats and absolute positioning hacks. Test in multiple browsers and screen sizes. Use browser DevTools to inspect and debug styles — they are your most powerful CSS tool.`, keyPoints: ['Use a naming convention (BEM, utility-first, etc.).', 'Organize styles: reset → tokens → globals → components.', 'Keep selectors flat and class-based, avoid deep nesting.', 'Mobile-first: base styles for small screens, enhance upward.', 'Use browser DevTools to inspect and debug CSS.', 'Avoid !important — it creates specificity nightmares.'] }
  ]
};
