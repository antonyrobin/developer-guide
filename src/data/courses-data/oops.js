export const oopsCourse = {
  id: 'oops', title: 'OOPs', description: 'Object-Oriented Programming principles and patterns.',
  officialDocs: null, tutorialLink: 'https://www.tutorialspoint.com/object_oriented_analysis_design/index.htm', exerciseLink: 'https://exercism.org/',
  sections: [
    { title: 'What is OOP', content: `Object-Oriented Programming (OOP) is a programming paradigm based on the concept of "objects" — entities that bundle data (attributes/properties) and behavior (methods/functions) together. Instead of thinking about code as a series of procedures, OOP models real-world entities and their interactions.\n\nOOP was popularized by languages like Smalltalk, C++, and Java in the 1980s-90s. Today, it is supported by virtually every major language: Java, C#, Python, JavaScript, TypeScript, PHP, Swift, Kotlin, and many more.\n\nThe four fundamental principles of OOP — Encapsulation, Abstraction, Inheritance, and Polymorphism — provide a framework for organizing complex systems into manageable, reusable, and extensible components.`, keyPoints: ['OOP bundles data and behavior into objects.', 'Four pillars: Encapsulation, Abstraction, Inheritance, Polymorphism.', 'Models real-world entities and interactions.'] },
    { title: 'Classes & Objects', content: `A Class is a blueprint (template) that defines the structure and behavior of objects. An Object is an instance — a concrete realization of a class with specific data values.\n\nThink of "Car" as a class: it defines properties (color, brand, speed) and methods (accelerate, brake). A specific red Tesla Model 3 is an object — an instance of the Car class with specific values.\n\nClasses typically contain: a Constructor (initializes the object's state when created), Properties/Fields (data the object holds), and Methods (actions the object can perform). Most languages use the "new" keyword to create instances.`, code: `// JavaScript ES6 Class\nclass User {\n  constructor(name, email) {\n    this.name = name;\n    this.email = email;\n    this.isActive = true;\n  }\n\n  deactivate() {\n    this.isActive = false;\n    console.log(\`\${this.name} deactivated.\`);\n  }\n\n  toString() {\n    return \`\${this.name} (\${this.email})\`;\n  }\n}\n\nconst alice = new User("Alice", "alice@example.com");\nalice.deactivate();`, codeLabel: 'Class & Object Example', keyPoints: ['Class = blueprint, Object = instance with real data.', 'Constructor initializes object state.', 'Objects combine properties (data) and methods (behavior).'] },
    { title: 'Encapsulation', content: `Encapsulation is the practice of bundling data and the methods that operate on that data within a single unit (class), while restricting direct access to some of the object's components. This is like a capsule that hides internal mechanics.\n\nWhy? Without encapsulation, any part of the code can modify an object's internal state — leading to unpredictable bugs. By making fields private and exposing controlled access via public methods (getters/setters), you enforce validation rules and maintain data integrity.\n\nFor example, a BankAccount class should never let external code set the balance directly. Instead, deposit() and withdraw() methods validate amounts, check for overdrafts, and maintain an audit trail.`, code: `class BankAccount {\n  #balance; // Private field (JS)\n\n  constructor(initialBalance) {\n    this.#balance = initialBalance;\n  }\n\n  deposit(amount) {\n    if (amount <= 0) throw new Error("Amount must be positive");\n    this.#balance += amount;\n    return this.#balance;\n  }\n\n  withdraw(amount) {\n    if (amount > this.#balance) throw new Error("Insufficient funds");\n    this.#balance -= amount;\n    return this.#balance;\n  }\n\n  get balance() {\n    return this.#balance; // Read-only access\n  }\n}\n\nconst account = new BankAccount(1000);\naccount.deposit(500);  // OK\n// account.#balance = 0; // ERROR: private field`, codeLabel: 'Encapsulation Example', keyPoints: ['Hide internal state; expose controlled access.', 'Private fields prevent external corruption.', 'Getters/setters enforce validation rules.'] },
    { title: 'Inheritance', content: `Inheritance allows a new class (child/subclass) to inherit properties and methods from an existing class (parent/superclass). The child class can reuse parent code and add its own unique features.\n\nThis creates an "is-a" relationship: a Dog IS-A Animal. A Manager IS-A Employee. The child inherits all parent behaviors but can override them to provide specialized implementations.\n\nHowever, deep inheritance hierarchies (5+ levels) become fragile and difficult to modify. The general modern recommendation is to "favor composition over inheritance" — building objects from interchangeable parts rather than deep family trees.`, code: `class Animal {\n  constructor(name) { this.name = name; }\n  speak() { return \`\${this.name} makes a noise.\`; }\n}\n\n// Dog inherits from Animal\nclass Dog extends Animal {\n  speak() {\n    return \`\${this.name} barks!\`; // Override parent method\n  }\n  fetch(item) {\n    return \`\${this.name} fetches the \${item}.\`; // New method\n  }\n}\n\nconst rex = new Dog("Rex");\nrex.speak();        // "Rex barks!"\nrex.fetch("ball");  // "Rex fetches the ball."\nrex instanceof Dog;    // true\nrex instanceof Animal; // true (is-a relationship)`, codeLabel: 'Inheritance Example', keyPoints: ['Child class inherits parent properties and methods.', 'Methods can be overridden for specialized behavior.', 'Creates an "is-a" relationship.', 'Avoid deep hierarchies — favor composition instead.'] },
    { title: 'Polymorphism', content: `Polymorphism means "many forms." It allows objects of different classes to be treated uniformly through a shared interface. A single method call can produce different results depending on the object's actual type.\n\nMethod overriding is the most common form: multiple classes implement the same method name differently. A Shape class might have an area() method, and Circle, Rectangle, and Triangle each implement it with their own formula. Code that works with shapes calls shape.area() without knowing or caring about the specific shape type.\n\nThis is incredibly powerful for extensibility — you can add new shape types without changing existing code that uses shapes.`, code: `class Shape {\n  area() { throw new Error("Must implement area()"); }\n}\n\nclass Circle extends Shape {\n  constructor(radius) { super(); this.radius = radius; }\n  area() { return Math.PI * this.radius ** 2; }\n}\n\nclass Rectangle extends Shape {\n  constructor(w, h) { super(); this.width = w; this.height = h; }\n  area() { return this.width * this.height; }\n}\n\n// Polymorphism: same method, different behavior\nconst shapes = [new Circle(5), new Rectangle(4, 6)];\nshapes.forEach(shape => {\n  console.log(shape.area()); // 78.54, 24 — each calculates differently\n});`, codeLabel: 'Polymorphism Example', keyPoints: ['Same method name, different implementations.', 'Code works with the base type, not specific classes.', 'Enables extensibility without modifying existing code.'] },
    { title: 'Abstraction', content: `Abstraction hides complex implementation details and exposes only the essential interface. Users interact with a simplified "what it does" without needing to understand "how it does it."\n\nEvery time you drive a car, you use abstraction: the steering wheel, pedals, and gear shift are the interface. The engine, transmission, and fuel injection are hidden. You don't need to understand combustion to drive.\n\nIn programming, abstract classes and interfaces define contracts: "any class implementing this interface MUST have these methods." This allows teams to work independently — one team builds the interface consumer, another builds the implementation — and swap implementations without breaking callers.\n\nJavaScript uses duck typing ("if it walks like a duck and quacks like a duck, it's a duck"), which provides abstraction through convention rather than formal interfaces. TypeScript adds formal interface support.`, code: `// TypeScript Interface (formal abstraction)\ninterface PaymentProcessor {\n  charge(amount: number): Promise<boolean>;\n  refund(transactionId: string): Promise<boolean>;\n}\n\n// Multiple implementations\nclass StripeProcessor implements PaymentProcessor {\n  async charge(amount) { /* Stripe-specific logic */ }\n  async refund(id) { /* Stripe refund logic */ }\n}\n\nclass PayPalProcessor implements PaymentProcessor {\n  async charge(amount) { /* PayPal-specific logic */ }\n  async refund(id) { /* PayPal refund logic */ }\n}\n\n// Consumer doesn't care which processor\nasync function checkout(processor: PaymentProcessor, amount: number) {\n  return processor.charge(amount); // Works with any implementation\n}`, codeLabel: 'Abstraction with Interfaces', keyPoints: ['Hide complexity, expose only the essential interface.', 'Interfaces define contracts without implementation details.', 'Enables swapping implementations without breaking callers.', 'JavaScript uses duck typing; TypeScript adds formal interfaces.'] },
    { title: 'SOLID Principles', content: `SOLID is a set of five design principles that make software more understandable, flexible, and maintainable. They were coined by Robert C. Martin ("Uncle Bob").\n\nS — Single Responsibility: A class should have only one reason to change. An EmailService should not also handle database queries.\n\nO — Open/Closed: Classes should be open for extension but closed for modification. Use inheritance or composition to add new behavior without changing existing tested code.\n\nL — Liskov Substitution: Objects of a superclass should be replaceable with objects of a subclass without breaking the application. If a function expects an Animal, passing a Dog should work perfectly.\n\nI — Interface Segregation: Don't force classes to implement methods they don't use. Split large interfaces into smaller, focused ones.\n\nD — Dependency Inversion: High-level modules should not depend on low-level modules. Both should depend on abstractions (interfaces). This enables swapping implementations and makes code testable.`, keyPoints: ['Single Responsibility: one class, one reason to change.', 'Open/Closed: extend behavior without modifying existing code.', 'Liskov Substitution: subtypes must be interchangeable.', 'Interface Segregation: small, focused interfaces.', 'Dependency Inversion: depend on abstractions, not implementations.'] },
    { title: 'Composition vs Inheritance', content: `The modern recommendation is to "favor composition over inheritance." While inheritance creates rigid "is-a" hierarchies, composition creates flexible "has-a" relationships by assembling objects from smaller, interchangeable parts.\n\nInheritance problems: Deep hierarchies are fragile (changing a parent breaks all children). The "banana problem" — you wanted a banana but got a gorilla holding the banana and the entire jungle (inheriting unwanted behavior). Multiple inheritance is dangerous (the "diamond problem").\n\nComposition builds complex objects by combining simple, focused objects. A Player in a game doesn't inherit from Movable, Damageable, and Renderable — it contains instances of MovementSystem, HealthSystem, and RenderSystem. Each system can be developed, tested, and swapped independently.\n\nIn JavaScript, composition is natural because objects can be assembled from mixins, higher-order functions, or module imports without class hierarchies.`, code: `// Composition over Inheritance\nconst canMove = (state) => ({\n  move: (x, y) => { state.x += x; state.y += y; }\n});\n\nconst canShoot = (state) => ({\n  shoot: () => console.log(\`\${state.name} fires!\`)\n});\n\nconst canHeal = (state) => ({\n  heal: (amount) => { state.hp += amount; }\n});\n\n// Compose a Player from parts\nfunction createPlayer(name) {\n  const state = { name, x: 0, y: 0, hp: 100 };\n  return {\n    ...canMove(state),\n    ...canShoot(state),\n    ...canHeal(state),\n    getState: () => ({ ...state }),\n  };\n}\n\nconst player = createPlayer("Hero");\nplayer.move(10, 5);\nplayer.shoot();\nplayer.heal(20);`, codeLabel: 'Composition Pattern', keyPoints: ['Inheritance: "is-a" (rigid). Composition: "has-a" (flexible).', 'Composition allows mixing behaviors without hierarchies.', 'Each composed part can be tested and swapped independently.', 'Modern JS naturally supports composition via functions/modules.'] }
  ]
};
