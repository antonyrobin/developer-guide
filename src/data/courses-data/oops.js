export const oopsCourse = {
  id: 'oops', title: 'OOPs', description: 'Object-Oriented Programming principles and patterns.',
  officialDocs: null, tutorialLink: 'https://www.tutorialspoint.com/object_oriented_analysis_design/index.htm', exerciseLink: 'https://exercism.org/',
  sections: [
    { title: 'What is OOP', content: `**Object-Oriented Programming** (**OOP**) is one of the most important programming paradigms used in modern software development. It helps developers design software in a *structured*, *modular*, and *reusable* way by modeling programs around real-world objects. Languages such as **Java**, **C++**, **Python**, **C#**, and **JavaScript** strongly support OOP concepts.\n\nOOP is a programming paradigm that organizes software design around objects, which represent real-world entities. Each object contains **Data** (variables/attributes) and **Behavior** (methods/functions). OOP focuses on creating *reusable*, *maintainable*, *secure*, and *scalable* applications.\n\n### Why OOP Matters\n\nOOP plays a crucial role in software development because it improves **code reusability**, enhances *maintainability* of programs, makes applications **scalable**, helps in real-world modeling of problems, and reduces code complexity.\n\n### Real-World Analogy\n\nThink of a car. A car has data (color, speed, fuel) and behavior (drive, brake). Similarly, objects in OOP combine data and behavior together.\n\n### Core Pillars\n\nThe core concepts of OOP include: **Class**, **Object**, **Encapsulation**, **Abstraction**, **Inheritance**, **Polymorphism**, and **Interface**. These principles provide a framework for organizing complex systems into manageable, reusable, and extensible components.`, image: '/images/oops/oops-core-concepts.svg', keyPoints: ['OOP bundles data and behavior into objects.', 'Core concepts: Class, Object, Encapsulation, Abstraction, Inheritance, Polymorphism, Interface.', 'Improves code reusability and maintainability.', 'Helps in real-world modeling of problems.', 'Supported by Java, C++, Python, C#, JavaScript, and many more.'] },
    { title: 'Class', content: `A **Class** is a *blueprint* or *template* used to create objects. It defines the structure and behavior that objects of that type will have. A class defines variables (data members), methods (member functions), and importantly — memory is not allocated when a class is created, only when objects are instantiated from it.\n\nThink of a class like an architectural blueprint of a house. The blueprint is not a real house, but it defines exactly how the house will look — its rooms, dimensions, and layout. Similarly, a class defines properties (data) and methods (actions) but doesn't occupy memory for actual data until an object is created.\n\n### Class Components\n\nClasses typically contain: a **Constructor** (initializes the object\u2019s state when created), **Properties/Fields** (data the object holds), and **Methods** (actions the object can perform). A class like "Car" would define properties such as color, weight, speed, and model, along with methods like startCar(), changeGear(), slowDown(), and brake().`, image: '/images/oops/oops-class-object.svg', code: `// Java Example\nclass Student {\n  int id;\n  String name;\n\n  void display() {\n    System.out.println(id + " " + name);\n  }\n}\n\n// JavaScript ES6 Class\nclass User {\n  constructor(name, email) {\n    this.name = name;\n    this.email = email;\n    this.isActive = true;\n  }\n\n  deactivate() {\n    this.isActive = false;\n    console.log(\`\${this.name} deactivated.\`);\n  }\n\n  toString() {\n    return \`\${this.name} (\${this.email})\`;\n  }\n}`, codeLabel: 'Class Definition — Java & JavaScript', keyPoints: ['Class is a blueprint or template for creating objects.', 'Defines variables (data members) and methods (member functions).', 'Memory is NOT allocated when a class is created.', 'Constructor initializes object state when instantiated.', 'Real-life analogy: A class is like a blueprint of a house.'] },
    { title: 'Object', content: `An **Object** is an instance of a class — a concrete realization with specific data values. When an object is created, memory is allocated and the **constructor** initializes its state. Objects represent real-world entities, and multiple objects can be created from a single class.\n\n### Blueprint to Reality\n\nIf a class is a blueprint of a house, objects are the *actual houses* built using that blueprint. Each house (object) has its own specific color, size, and address — even though they share the same structural design (class). A "Car" class can produce objects like Ford and Toyota, each with their own property values but sharing the same structure and methods.\n\n### Creating Objects\n\nMost languages use the \`new\` keyword to create instances. Each object maintains its own copy of instance variables, but shares the method definitions defined in the class. This is what makes OOP memory-efficient — the code is shared, only the data is unique per object.`, image: '/images/oops/oops-class-object.svg', code: `// Java Example\npublic class Main {\n  public static void main(String[] args) {\n    Student s1 = new Student();\n    s1.id = 101;\n    s1.name = "Sarsha";\n    s1.display();\n  }\n}\n\n// JavaScript Example\nconst alice = new User("Alice", "alice@example.com");\nconst bob = new User("Bob", "bob@example.com");\n\nalice.deactivate();  // "Alice deactivated."\nconsole.log(bob.toString()); // "Bob (bob@example.com)"`, codeLabel: 'Creating Objects — Java & JavaScript', keyPoints: ['Object is an instance of a class with real data.', 'Memory is allocated when an object is created.', 'Multiple objects can be created from a single class.', 'Each object has its own copy of instance variables.', 'Real-life analogy: If class is blueprint, object is the actual house.'] },
    { title: 'Encapsulation', content: `**Encapsulation** is the process of wrapping data and methods together into a single unit (class), while protecting data using **access modifiers**. It achieves **data hiding** — variables are declared as \`private\` and accessed only through \`public\` getter and setter methods.\n\n### Why Encapsulation?\n\nWithout encapsulation, any part of the code can modify an object's internal state — leading to unpredictable bugs. By making fields \`private\` and exposing controlled access via public methods (*getters/setters*), you enforce validation rules and maintain **data integrity**.\n\nFor example, a \`BankAccount\` class should never let external code set the balance directly. Instead, \`deposit()\` and \`withdraw()\` methods validate amounts, check for overdrafts, and maintain an audit trail.\n\n### Real-World Analogy\n\nAn **ATM machine** hides internal details. You can withdraw money, but you cannot directly access the bank's database. The ATM provides a controlled interface (buttons, screen) while hiding the complex banking operations behind it.`, image: '/images/oops/oops-encapsulation.svg', code: `// Java Example\nclass BankAccount {\n  private double balance;\n\n  public void setBalance(double bal) {\n    balance = bal;\n  }\n\n  public double getBalance() {\n    return balance;\n  }\n}\n\n// JavaScript Example (ES2022 Private Fields)\nclass BankAccountJS {\n  #balance; // Private field\n\n  constructor(initialBalance) {\n    this.#balance = initialBalance;\n  }\n\n  deposit(amount) {\n    if (amount <= 0) throw new Error("Amount must be positive");\n    this.#balance += amount;\n    return this.#balance;\n  }\n\n  withdraw(amount) {\n    if (amount > this.#balance) throw new Error("Insufficient funds");\n    this.#balance -= amount;\n    return this.#balance;\n  }\n\n  get balance() {\n    return this.#balance; // Read-only access\n  }\n}`, codeLabel: 'Encapsulation — Java & JavaScript', keyPoints: ['Wraps data and methods together, restricts direct access.', 'Variables are declared as private for data hiding.', 'Accessed using public getter and setter methods.', 'Enforces validation rules and maintains data integrity.', 'Real-life analogy: ATM hides internal banking operations.'] },
    { title: 'Abstraction', content: `**Abstraction** is the process of hiding implementation details and showing only essential features to the user. It focuses on *"what to do"* rather than *"how to do it."* Abstraction is achieved using **abstract classes** and **interfaces**.\n\n### Real-World Analogy\n\nEvery time you drive a car, you use abstraction: the steering wheel, pedals, and gear shift are the interface. The engine, transmission, and fuel injection are hidden. You don't need to understand combustion to drive.\n\n### Contracts in Code\n\nIn programming, **abstract classes** and **interfaces** define contracts: "any class implementing this interface MUST have these methods." This allows teams to work independently — one team builds the interface consumer, another builds the implementation — and swap implementations without breaking callers.\n\n### Duck Typing\n\nJavaScript uses *duck typing* ("if it walks like a duck and quacks like a duck, it's a duck"), which provides abstraction through convention rather than formal interfaces. TypeScript adds formal interface support.`, image: '/images/oops/oops-abstraction.svg', code: `// Java Example — Abstract Class\nabstract class Shape {\n  abstract void draw();\n}\n\nclass Circle extends Shape {\n  void draw() {\n    System.out.println("Drawing Circle");\n  }\n}\n\n// TypeScript Example — Interface (formal abstraction)\ninterface PaymentProcessor {\n  charge(amount: number): Promise<boolean>;\n  refund(transactionId: string): Promise<boolean>;\n}\n\nclass StripeProcessor implements PaymentProcessor {\n  async charge(amount) { /* Stripe-specific logic */ }\n  async refund(id) { /* Stripe refund logic */ }\n}\n\nclass PayPalProcessor implements PaymentProcessor {\n  async charge(amount) { /* PayPal-specific logic */ }\n  async refund(id) { /* PayPal refund logic */ }\n}\n\n// Consumer doesn't care which processor\nasync function checkout(processor: PaymentProcessor, amount: number) {\n  return processor.charge(amount); // Works with any implementation\n}`, codeLabel: 'Abstraction — Java & TypeScript', keyPoints: ['Hides implementation details, shows only essential features.', 'Focuses on "what to do" not "how to do it".', 'Achieved using abstract classes and interfaces.', 'Enables swapping implementations without breaking callers.', 'Real-life analogy: Driving a car without knowing engine internals.'] },
    { title: 'Inheritance', content: `**Inheritance** allows one class (*child/subclass*) to acquire properties and methods of another class (*parent/superclass*). It promotes **code reusability** and uses the \`extends\` keyword. The parent class is called Superclass and the child class is called Subclass.\n\nThis creates an "is-a" relationship: a Dog IS-A Animal. A Manager IS-A Employee. The child inherits all parent behaviors but can override them to provide specialized implementations.\n\n### Types of Inheritance\n\na) **Single Inheritance** — One child class inherits from one parent class (e.g., Manager extends Employee).\n\nb) **Multilevel Inheritance** — A chain of inheritance (e.g., Person → Employee → Manager).\n\nc) **Hierarchical Inheritance** — Multiple child classes inherit from one parent (e.g., Car and Bike both extend Vehicle).\n\nd) **Multiple Inheritance** — A class inherits from multiple parents. Java doesn't support this with classes (to avoid the "diamond problem") but supports it through interfaces (e.g., Smartphone implements Camera, MusicPlayer).\n\nHowever, deep inheritance hierarchies (5+ levels) become fragile and difficult to modify. The general modern recommendation is to "favor composition over inheritance."`, image: '/images/oops/oops-inheritance-types.svg', code: `// Java — Single Inheritance\nclass Employee {\n  void work() { System.out.println("Employee works"); }\n}\nclass Manager extends Employee {\n  void manage() { System.out.println("Manager manages team"); }\n}\n\n// Java — Multiple Inheritance via Interfaces\ninterface Camera {\n  void takePhoto();\n}\ninterface MusicPlayer {\n  void playMusic();\n}\nclass Smartphone implements Camera, MusicPlayer {\n  public void takePhoto() { System.out.println("Taking photo"); }\n  public void playMusic() { System.out.println("Playing music"); }\n}\n\n// JavaScript — Inheritance\nclass Animal {\n  constructor(name) { this.name = name; }\n  speak() { return \`\${this.name} makes a noise.\`; }\n}\nclass Dog extends Animal {\n  speak() { return \`\${this.name} barks!\`; }\n  fetch(item) { return \`\${this.name} fetches the \${item}.\`; }\n}\n\nconst rex = new Dog("Rex");\nrex.speak();        // "Rex barks!"\nrex instanceof Animal; // true (is-a relationship)`, codeLabel: 'Inheritance Types — Java & JavaScript', keyPoints: ['Child class inherits parent properties and methods.', 'Single: one parent → one child. Multilevel: chain of inheritance.', 'Hierarchical: one parent → multiple children.', 'Multiple inheritance via interfaces (avoids diamond problem).', 'Uses "extends" for classes, "implements" for interfaces.'] },
    { title: 'Polymorphism', content: `**Polymorphism** means *"many forms"* — it allows a single method to behave differently depending on the object that calls it. A single method call can produce different results depending on the object's actual type.\n\n### Two Types of Polymorphism\n\na) **Compile-Time Polymorphism** (*Method Overloading*) — Same method name with different parameters within the same class. The compiler decides which method to call based on the argument types/count. For example, a Calculator class can have add(int a, int b) and add(int a, int b, int c).\n\nb) **Run-Time Polymorphism** (*Method Overriding*) — A subclass provides a different implementation of a method already defined in its parent class. The JVM decides which method to call at runtime based on the actual object type. For example, Dog overrides Animal's sound() method.\n\nReal-life analogy: A person can act as a student, employee, or customer depending on the situation — same person, different behaviors.\n\nThis is incredibly powerful for extensibility — you can add new types without changing existing code that uses the base type.`, image: '/images/oops/oops-polymorphism.svg', code: `// Java — Compile-Time (Method Overloading)\nclass Calculator {\n  int add(int a, int b) {\n    return a + b;\n  }\n  int add(int a, int b, int c) {\n    return a + b + c;\n  }\n}\n\n// Java — Run-Time (Method Overriding)\nclass Animal {\n  void sound() { System.out.println("Animal makes sound"); }\n}\nclass Dog extends Animal {\n  void sound() { System.out.println("Dog barks"); }\n}\n\n// JavaScript — Polymorphism\nclass Shape {\n  area() { throw new Error("Must implement area()"); }\n}\nclass Circle extends Shape {\n  constructor(radius) { super(); this.radius = radius; }\n  area() { return Math.PI * this.radius ** 2; }\n}\nclass Rectangle extends Shape {\n  constructor(w, h) { super(); this.width = w; this.height = h; }\n  area() { return this.width * this.height; }\n}\n\nconst shapes = [new Circle(5), new Rectangle(4, 6)];\nshapes.forEach(s => console.log(s.area())); // 78.54, 24`, codeLabel: 'Polymorphism — Java & JavaScript', keyPoints: ['Polymorphism means "many forms" of a single method.', 'Compile-Time: Method Overloading — same name, different parameters.', 'Run-Time: Method Overriding — subclass redefines parent method.', 'Enables extensibility without modifying existing code.', 'Real-life analogy: Same person acts as student, employee, or customer.'] },
    { title: 'Interface', content: `An **Interface** is a blueprint that contains *abstract methods* and supports **multiple inheritance**. It defines a contract — "what a class must do" — without specifying "how to do it." Interfaces use the \`implements\` keyword in Java.\n\nAll methods in an interface are \`public\` and \`abstract\` by default. A class that implements an interface MUST provide concrete implementations of all its methods. This enforces a consistent API across different implementations.\n\n### Java 8+ Features\n\nSince Java 8, interfaces can also contain \`default\` methods (with implementation) and \`static\` methods. Since Java 17, **sealed interfaces** restrict which classes can implement them.\n\n### Why Interfaces Matter\n\nInterfaces are particularly powerful for achieving **loose coupling** and **dependency inversion**. Code that depends on interfaces rather than concrete classes can easily swap implementations — for example, switching from a MySQL database to PostgreSQL without changing business logic.\n\nJavaScript doesn't have formal interfaces, but TypeScript adds full interface support. In plain JavaScript, duck typing and convention serve the same purpose.`, image: '/images/oops/oops-interface.svg', code: `// Java — Interface\ninterface Printable {\n  void print();\n}\n\nclass Document implements Printable {\n  public void print() {\n    System.out.println("Printing document");\n  }\n}\n\n// TypeScript — Interface\ninterface Cacheable {\n  get(key: string): Promise<any>;\n  set(key: string, value: any, ttl?: number): Promise<void>;\n  delete(key: string): Promise<boolean>;\n}\n\nclass RedisCache implements Cacheable {\n  async get(key) { /* Redis GET logic */ }\n  async set(key, value, ttl) { /* Redis SET logic */ }\n  async delete(key) { /* Redis DEL logic */ }\n}\n\nclass MemoryCache implements Cacheable {\n  private store = new Map();\n  async get(key) { return this.store.get(key); }\n  async set(key, value) { this.store.set(key, value); }\n  async delete(key) { return this.store.delete(key); }\n}`, codeLabel: 'Interface — Java & TypeScript', keyPoints: ['Interface is a blueprint with abstract methods.', 'All methods are public and abstract by default.', 'Uses "implements" keyword to apply the contract.', 'Supports multiple inheritance (a class can implement many interfaces).', 'Enables loose coupling and swappable implementations.'] },
    { title: 'SOLID Principles', content: `**SOLID** is a set of five design principles that make software more *understandable*, *flexible*, and *maintainable*. They were coined by **Robert C. Martin** (*"Uncle Bob"*).\n\nS — **Single Responsibility**: A class should have only one reason to change. An \`EmailService\` should not also handle database queries.\n\nO — **Open/Closed**: Classes should be *open for extension* but *closed for modification*. Use inheritance or composition to add new behavior without changing existing tested code.\n\nL — **Liskov Substitution**: Objects of a superclass should be replaceable with objects of a subclass without breaking the application. If a function expects an Animal, passing a Dog should work perfectly.\n\nI — **Interface Segregation**: Don't force classes to implement methods they don't use. Split large interfaces into smaller, focused ones.\n\nD — **Dependency Inversion**: High-level modules should not depend on low-level modules. Both should depend on *abstractions* (interfaces). This enables swapping implementations and makes code testable.`, keyPoints: ['Single Responsibility: one class, one reason to change.', 'Open/Closed: extend behavior without modifying existing code.', 'Liskov Substitution: subtypes must be interchangeable.', 'Interface Segregation: small, focused interfaces.', 'Dependency Inversion: depend on abstractions, not implementations.'] },
    { title: 'Composition vs Inheritance', content: `The modern recommendation is to **"favor composition over inheritance."** While inheritance creates rigid "is-a" hierarchies, composition creates flexible "has-a" relationships by assembling objects from smaller, interchangeable parts.\n\n### The Problems with Inheritance\n\nInheritance problems: Deep hierarchies are *fragile* (changing a parent breaks all children). The "banana problem" — you wanted a banana but got a gorilla holding the banana and the entire jungle (inheriting unwanted behavior). Multiple inheritance is dangerous (the "diamond problem").\n\n### How Composition Works\n\nComposition builds complex objects by combining simple, focused objects. A Player in a game doesn't inherit from Movable, Damageable, and Renderable — it contains instances of MovementSystem, HealthSystem, and RenderSystem. Each system can be developed, tested, and swapped independently.\n\nIn JavaScript, composition is natural because objects can be assembled from mixins, higher-order functions, or module imports without class hierarchies.`, code: `// Composition over Inheritance\nconst canMove = (state) => ({\n  move: (x, y) => { state.x += x; state.y += y; }\n});\n\nconst canShoot = (state) => ({\n  shoot: () => console.log(\`\${state.name} fires!\`)\n});\n\nconst canHeal = (state) => ({\n  heal: (amount) => { state.hp += amount; }\n});\n\n// Compose a Player from parts\nfunction createPlayer(name) {\n  const state = { name, x: 0, y: 0, hp: 100 };\n  return {\n    ...canMove(state),\n    ...canShoot(state),\n    ...canHeal(state),\n    getState: () => ({ ...state }),\n  };\n}\n\nconst player = createPlayer("Hero");\nplayer.move(10, 5);\nplayer.shoot();\nplayer.heal(20);`, codeLabel: 'Composition Pattern', keyPoints: ['Inheritance: "is-a" (rigid). Composition: "has-a" (flexible).', 'Composition allows mixing behaviors without hierarchies.', 'Each composed part can be tested and swapped independently.', 'Modern JS naturally supports composition via functions/modules.'] },
    { title: 'Advantages & Disadvantages', content: `### Advantages of OOP\n\n**Code Reusability** — Inheritance and composition allow reusing existing code across projects and modules, reducing duplication and development time.\n\n**Easy Maintenance** — Encapsulation isolates changes. Modifying one class doesn't break others if the public interface remains the same.\n\n**Improved Security** — Access modifiers (\`private\`, \`protected\`) prevent unauthorized access to sensitive data and internal state.\n\n**Better Scalability** — OOP's modular design makes it easier to scale applications by adding new classes and objects without disrupting existing functionality.\n\n**Real-World Problem Modeling** — OOP naturally maps to real-world entities and relationships, making systems easier to understand and communicate.\n\n### Disadvantages of OOP\n\n**Higher Complexity for Small Programs** — For simple scripts or small utilities, OOP adds unnecessary boilerplate and overhead compared to procedural programming.\n\n**Slower Execution** — Object creation, method dispatch, and memory management add runtime overhead compared to procedural code. This is often negligible for most applications but matters in performance-critical systems.\n\n**Requires More Memory** — Each object carries its own data copy and metadata, consuming more memory than simple data structures in procedural programs.\n\n### Conclusion\n\nObject-Oriented Programming provides a strong foundation for building real-world software applications. By understanding classes, objects, encapsulation, abstraction, inheritance, and polymorphism, developers can write clean, reusable, and efficient code. Mastering OOP is a key step toward becoming a successful software engineer.`, keyPoints: ['Advantages: reusability, maintainability, security, scalability.', 'OOP naturally models real-world entities and relationships.', 'Disadvantage: more complex for small/simple programs.', 'Slightly slower execution due to object overhead.', 'OOP is essential for building large-scale, enterprise software.'] },
    { title: 'Best Practices & Code Standards', content: `**Program to interfaces, not implementations.** When a class depends on an abstraction (interface) rather than a concrete class, you can swap implementations freely — for testing, for different environments, or when requirements change. This is the foundation of **dependency injection** and testable design.\n\n### Class Design\n\nApply the **Single Responsibility Principle** religiously — each class should have exactly one reason to change. If a class is doing too many things (validating, querying, emailing, logging), split it into focused collaborators. Name classes after what they *are*, not what they *do*.\n\n### Composition Over Inheritance\n\nPrefer **composition** for code reuse. Inheritance creates rigid hierarchies that are fragile and hard to change. Composition lets you assemble behavior from independent, interchangeable parts. Reserve inheritance for genuine "is-a" relationships with shared behavior, not just for code reuse.`, code: `// DO: Program to interfaces (abstractions)\ninterface Logger {\n  log(message: string): void;\n}\n\nclass ConsoleLogger implements Logger {\n  log(message: string) { console.log(message); }\n}\n\nclass FileLogger implements Logger {\n  log(message: string) { /* write to file */ }\n}\n\n// Service depends on abstraction — testable!\nclass OrderService {\n  constructor(private logger: Logger) {}\n  createOrder(data) {\n    // ... business logic\n    this.logger.log("Order created");\n  }\n}\n\n// DON'T: Depend on concrete classes\nclass OrderService2 {\n  private logger = new ConsoleLogger(); // Tight coupling!\n  // Cannot swap, cannot mock in tests\n}\n\n// DO: Small, focused classes (SRP)\nclass EmailValidator { validate(email) { /* ... */ } }\nclass PasswordValidator { validate(pwd) { /* ... */ } }\nclass AuthService {\n  constructor(emailVal, pwdVal) { /* injected */ }\n}\n\n// DON'T: God classes that do everything\nclass UserManager {\n  validateEmail() {}\n  hashPassword() {}\n  sendEmail() {}\n  generateReport() {}\n  // Hundreds of unrelated methods\n}`, codeLabel: 'Dos & Don\'ts', keyPoints: ['Program to interfaces, not concrete implementations.', 'Apply Single Responsibility — each class should have one job.', 'Prefer composition over inheritance for flexible designs.', 'Use dependency injection to decouple classes and enable testing.'] }
  ]
};
