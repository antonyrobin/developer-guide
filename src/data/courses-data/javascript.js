export const jsCourse = {
  id: 'javascript',
  title: 'JavaScript',
  description: 'The programming language of the web — interactive, dynamic, everywhere.',
  officialDocs: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript',
  tutorialLink: 'https://www.w3schools.com/js/',
  exerciseLink: 'https://www.w3schools.com/js/js_exercises.asp',
  sections: [
    { title: 'What is JavaScript', content: `JavaScript is a high-level, interpreted programming language that makes web pages dynamic and interactive. It is the only programming language that runs natively in web browsers, making it the de facto language of the web.\n\nOriginally created by Brendan Eich in just 10 days in 1995, JavaScript has evolved from a simple scripting language into a full-featured platform. With Node.js (2009), JavaScript broke free from the browser and can now run servers, command-line tools, desktop apps (Electron), mobile apps (React Native), and even IoT devices.\n\nJavaScript is dynamically typed (variables can hold any type), prototype-based (objects inherit from objects, not classes), event-driven (responds to user actions), and single-threaded (uses an event loop for concurrency). Despite early criticism, modern JavaScript (ES6+) is a powerful, elegant language.\n\nThe ECMAScript specification (maintained by TC39) standardizes JavaScript. Major versions: ES5 (2009) standardized the language, ES6/ES2015 introduced revolutionary features (let/const, arrow functions, classes, promises, modules), and annual releases since then continue adding features.`, image: 'https://images.unsplash.com/photo-1555099962-4199c345e5dd?w=800&auto=format&fit=crop&q=60', keyPoints: ['Only language that runs natively in browsers.', 'Node.js enables server-side JavaScript.', 'Dynamically typed, event-driven, single-threaded.', 'ES6 (2015) was a transformative update.'] },
    { title: 'Variables & Data Types', content: `JavaScript has three ways to declare variables. let declares a block-scoped variable that can be reassigned. const declares a block-scoped variable that cannot be reassigned (but objects/arrays it holds CAN be mutated). var is the legacy keyword — it is function-scoped and hoisted, leading to subtle bugs. Rule: always use const by default, use let when you need to reassign, never use var.\n\nPrimitive types are immutable values: String ("hello"), Number (42, 3.14 — no separate integer type), BigInt (for very large numbers), Boolean (true/false), undefined (variable declared but not assigned), null (intentionally empty), and Symbol (unique identifiers).\n\nReference types are mutable collections: Object ({key: value} — the foundation of JavaScript), Array ([1, 2, 3] — ordered lists, technically objects), Function (first-class citizens — can be stored in variables, passed as arguments, returned from other functions), Date, Map, Set, and RegExp.\n\nType coercion is JavaScript's automatic type conversion. It causes notorious quirks: "5" + 3 = "53" (string concatenation) but "5" - 3 = 2 (numeric subtraction). Use === (strict equality) instead of == (loose equality with coercion) to avoid surprises.`, code: `// Variable declarations\nconst API_URL = "https://api.example.com"; // Cannot reassign\nlet count = 0;                              // Can reassign\ncount = count + 1;\n\n// Primitive types\nconst name = "Alice";          // String\nconst age = 28;                // Number\nconst isActive = true;         // Boolean\nconst nothing = null;          // Null\nlet notYet;                    // undefined\n\n// Reference types\nconst user = { name: "Bob", age: 30 };  // Object\nconst colors = ["red", "blue", "green"]; // Array\nconst greet = (name) => \`Hello \${name}\`; // Function\n\n// Type checking\ntypeof "hello"    // "string"\ntypeof 42         // "number"\ntypeof null       // "object" (famous JS bug)\nArray.isArray([]) // true`, codeLabel: 'Variables & Types', keyPoints: ['Use const by default, let when reassignment needed.', 'Never use var — it has scoping issues.', '7 primitive types + Object reference type.', 'Always use === for comparison (strict equality).'] },
    { title: 'Functions & Scope', content: `Functions are the fundamental building blocks of JavaScript. They encapsulate reusable logic and are "first-class citizens" — meaning they can be assigned to variables, passed as arguments, and returned from other functions.\n\nFunction declarations are hoisted (available before their declaration line). Function expressions and arrow functions are not hoisted. Arrow functions (=>) are concise and do not have their own "this" binding — they inherit "this" from the surrounding scope, which is usually what you want.\n\nScope determines where variables are accessible. Global scope: accessible everywhere (avoid polluting it). Function scope: variables inside a function stay inside. Block scope (let/const): variables inside {} stay inside that block. Closures occur when an inner function "remembers" variables from its outer function even after the outer function has returned.\n\nDefault parameters set fallback values: function greet(name = "World"). Rest parameters (...args) collect remaining arguments into an array. Destructuring in parameters extracts specific properties: function render({ title, body }).`, code: `// Arrow function\nconst add = (a, b) => a + b;\n\n// Default parameters\nconst greet = (name = "World") => \`Hello, \${name}!\`;\n\n// Destructuring parameters\nconst displayUser = ({ name, age }) => {\n  console.log(\`\${name} is \${age} years old\`);\n};\ndisplayUser({ name: "Alice", age: 28 });\n\n// Closure — inner function remembers outer variable\nfunction createCounter() {\n  let count = 0; // Private variable\n  return {\n    increment: () => ++count,\n    getCount: () => count,\n  };\n}\nconst counter = createCounter();\ncounter.increment(); // 1\ncounter.increment(); // 2\ncounter.getCount();  // 2\n\n// Higher-order function (takes a function as argument)\nconst numbers = [1, 2, 3, 4, 5];\nconst doubled = numbers.map(n => n * 2); // [2, 4, 6, 8, 10]`, codeLabel: 'Function Patterns', keyPoints: ['Arrow functions do not have their own "this".', 'Use closures for data privacy and encapsulation.', 'Destructuring in parameters makes code clean.', 'Higher-order functions (map, filter, reduce) are essential.'] },
    { title: 'Arrays & Array Methods', content: `Arrays are ordered collections of values. Modern JavaScript provides powerful built-in methods that make array manipulation concise and expressive. These methods are essential to learn — they are used constantly.\n\nTransformation methods (return a new array): map() transforms each element. filter() keeps elements that pass a test. slice() extracts a portion. flat() / flatMap() flattens nested arrays. concat() merges arrays. The spread operator [...arr1, ...arr2] is a modern alternative to concat.\n\nReduction methods: reduce() accumulates a single value from all elements — it's the Swiss Army knife of array methods. find() returns the first matching element. findIndex() returns its index. some() checks if any element passes. every() checks if all pass.\n\nMutation methods (change the original array): push()/pop() add/remove from end. unshift()/shift() add/remove from start. splice() inserts/removes at any position. sort() sorts in place (WARNING: sorts as strings by default — use a comparator for numbers). reverse() reverses in place.\n\nDestructuring extracts values: const [first, ...rest] = array. The spread operator copies arrays: const copy = [...original]. For unique values, convert to Set: [...new Set(arrayWithDupes)].`, code: `const users = [\n  { name: "Alice", age: 28, role: "dev" },\n  { name: "Bob", age: 35, role: "pm" },\n  { name: "Carol", age: 24, role: "dev" },\n];\n\n// filter: keep only devs\nconst devs = users.filter(u => u.role === "dev");\n\n// map: extract names\nconst names = users.map(u => u.name); // ["Alice", "Bob", "Carol"]\n\n// find: first match\nconst bob = users.find(u => u.name === "Bob");\n\n// reduce: calculate total age\nconst totalAge = users.reduce((sum, u) => sum + u.age, 0); // 87\n\n// sort: by age (ascending)\nconst sorted = [...users].sort((a, b) => a.age - b.age);\n\n// Chaining methods\nconst devNames = users\n  .filter(u => u.role === "dev")\n  .map(u => u.name)\n  .sort(); // ["Alice", "Carol"]\n\n// Destructuring\nconst [first, ...rest] = devNames; // first="Alice", rest=["Carol"]\n\n// Remove duplicates\nconst unique = [...new Set([1, 2, 2, 3, 3])]; // [1, 2, 3]`, codeLabel: 'Array Methods Cheat Sheet', keyPoints: ['map/filter/reduce are the three most important methods.', 'These methods return new arrays (immutable pattern).', 'sort() converts to strings by default — always use a comparator.', 'Method chaining creates elegant data pipelines.'] },
    { title: 'DOM Manipulation', content: `The Document Object Model (DOM) is the browser's representation of an HTML page as a tree of objects. JavaScript can read, modify, add, or remove any element in this tree — making the page dynamic.\n\nSelecting elements: document.getElementById("id") selects by ID (fastest). document.querySelector(".class") selects the first match for any CSS selector. document.querySelectorAll("p") selects all matches and returns a NodeList.\n\nModifying elements: element.textContent changes text (safe from XSS). element.innerHTML changes HTML content (careful — user input can inject scripts). element.style.color = "red" sets inline styles. element.classList.add("active"), .remove("active"), .toggle("active") manipulate CSS classes.\n\nCreating elements: document.createElement("div") creates a new element in memory. Set its content and attributes, then append it to the DOM with parent.appendChild(element) or parent.append(element). Use element.remove() to delete elements.\n\nEvent handling: element.addEventListener("click", handler) is the modern way to listen for events. Events bubble up the DOM tree by default — clicking a button inside a div fires the handler on both. Use event.stopPropagation() to prevent bubbling, and event delegation (listening on a parent for all children) for performance.`, code: `// Selecting elements\nconst title = document.getElementById("page-title");\nconst cards = document.querySelectorAll(".card");\nconst firstBtn = document.querySelector("button.primary");\n\n// Modifying content and styles\ntitle.textContent = "Updated Title";\ntitle.classList.add("highlight");\ntitle.style.color = "var(--primary)";\n\n// Creating and appending elements\nconst newCard = document.createElement("div");\nnewCard.className = "card";\nnewCard.innerHTML = \`\n  <h3>New Card</h3>\n  <p>Dynamically created content.</p>\n\`;\ndocument.querySelector(".grid").appendChild(newCard);\n\n// Event handling\nfirstBtn.addEventListener("click", (event) => {\n  event.preventDefault();\n  console.log("Button clicked!", event.target);\n});\n\n// Event delegation (one listener for many items)\ndocument.querySelector(".card-list").addEventListener("click", (e) => {\n  if (e.target.matches(".delete-btn")) {\n    e.target.closest(".card").remove();\n  }\n});`, codeLabel: 'DOM Interaction Patterns', keyPoints: ['querySelector/querySelectorAll work with any CSS selector.', 'Use textContent (safe) over innerHTML (XSS risk).', 'classList.toggle() is perfect for interactive UI states.', 'Event delegation reduces memory by using one listener.'] },
    { title: 'Async JavaScript (Promises & Async/Await)', content: `JavaScript is single-threaded — it can only do one thing at a time. But web apps need to fetch data, read files, and handle timers without freezing the UI. This is where asynchronous programming comes in.\n\nThe Event Loop is the engine. Synchronous code runs on the Call Stack. Async operations (fetch, setTimeout) are handed off to Web APIs, and their callbacks are queued. When the Call Stack is empty, the Event Loop pushes the next callback onto the stack. This is how JS handles concurrency without threads.\n\nPromises represent a value that will be available in the future. A Promise is in one of three states: Pending (waiting), Fulfilled (success), or Rejected (error). Use .then() for success and .catch() for errors. Promises solve "callback hell" — deeply nested callbacks that are impossible to read.\n\nAsync/Await (ES2017) is syntactic sugar over Promises that makes async code look synchronous. Mark a function as async, then use await before any Promise. The code pauses at await until the Promise resolves, but the browser is NOT blocked — other events can still be processed.\n\nFor error handling, wrap await calls in try/catch blocks. Promise.all() runs multiple independent async operations in parallel — much faster than awaiting them sequentially. Promise.race() returns the result of whichever Promise settles first.`, code: `// Fetch API with async/await\nasync function getUser(id) {\n  try {\n    const response = await fetch(\`/api/users/\${id}\`);\n\n    if (!response.ok) {\n      throw new Error(\`HTTP error: \${response.status}\`);\n    }\n\n    const user = await response.json();\n    return user;\n\n  } catch (error) {\n    console.error("Failed to fetch user:", error.message);\n    return null;\n  }\n}\n\n// Parallel requests with Promise.all\nasync function getDashboard() {\n  const [user, posts, notifications] = await Promise.all([\n    fetch("/api/user").then(r => r.json()),\n    fetch("/api/posts").then(r => r.json()),\n    fetch("/api/notifications").then(r => r.json()),\n  ]);\n  return { user, posts, notifications };\n}\n\n// Sequential vs Parallel\nconst t1 = await fetchA(); // Takes 2s\nconst t2 = await fetchB(); // Takes 2s\n// Total: 4 seconds (sequential)\n\nconst [r1, r2] = await Promise.all([fetchA(), fetchB()]);\n// Total: 2 seconds (parallel)`, codeLabel: 'Async Patterns', keyPoints: ['The Event Loop handles concurrency in single-threaded JS.', 'async/await makes async code look synchronous.', 'Always wrap await in try/catch for error handling.', 'Promise.all() runs independent requests in parallel.'] },
    { title: 'ES6+ Modern Features', content: `ES6 (ES2015) was a revolutionary update. Here are the most important modern JavaScript features you should use daily.\n\nTemplate Literals use backticks for string interpolation: \`Hello \${name}\`. They support multi-line strings without concatenation and can contain any expression inside \${}.\n\nDestructuring extracts values from objects and arrays: const { name, age } = user; or const [first, second] = array. It works in function parameters, assignments, and nested structures.\n\nSpread (...) expands arrays/objects: [...arr1, ...arr2] merges arrays, { ...obj, newKey: value } clones and extends objects. Rest (...) collects remaining values: function sum(...numbers) collects all arguments into an array.\n\nOptional Chaining (?.) safely accesses nested properties: user?.address?.city returns undefined instead of crashing if any link is null. Nullish Coalescing (??) provides a default only for null/undefined: value ?? "default" (unlike ||, which also replaces 0 and "").\n\nModules (import/export) organize code into reusable files. Named exports allow multiple exports per file. Default exports are one per file. Dynamic imports (import()) load modules lazily at runtime.`, code: `// Template literals\nconst greeting = \`Welcome, \${user.name}! You have \${count} messages.\`;\n\n// Destructuring\nconst { name, age, address: { city } } = user;\nconst [first, , third] = [10, 20, 30]; // Skip second\n\n// Spread & Rest\nconst merged = { ...defaults, ...userConfig };\nconst sum = (...nums) => nums.reduce((a, b) => a + b, 0);\n\n// Optional chaining & nullish coalescing\nconst city = user?.profile?.address?.city ?? "Unknown";\nconst port = config.port ?? 3000; // Only null/undefined, not 0\n\n// Modules\n// math.js\nexport const add = (a, b) => a + b;\nexport const multiply = (a, b) => a * b;\n\n// app.js\nimport { add, multiply } from './math.js';\nimport config from './config.js'; // Default import`, codeLabel: 'Modern JavaScript Features', keyPoints: ['Template literals replace string concatenation.', 'Destructuring simplifies extracting data from objects/arrays.', 'Optional chaining (?.) prevents "Cannot read property" errors.', 'Use ?? instead of || for default values (it preserves 0 and "").'] },
    { title: 'Error Handling & Debugging', content: `Robust error handling is what separates production-quality code from prototypes. JavaScript provides try/catch/finally for synchronous errors and Promise .catch() / async try/catch for asynchronous errors.\n\nThe try block contains code that might throw an error. The catch block handles the error. The finally block runs regardless of success or failure — useful for cleanup (closing connections, hiding spinners).\n\nCustom error classes help categorize errors. Extend the built-in Error class: class ValidationError extends Error { constructor(msg) { super(msg); this.name = "ValidationError" } }. This allows catch blocks to check error types and respond differently.\n\nDebugging techniques: console.log() for quick checks. console.table() for arrays/objects. console.group()/groupEnd() for organized logging. The debugger statement pauses execution and opens browser DevTools automatically. Breakpoints in DevTools let you step through code line by line, inspect variables, and watch expressions.\n\nCommon error types: ReferenceError (using an undeclared variable), TypeError (calling a method on null/undefined), SyntaxError (code that cannot be parsed), and RangeError (value outside allowed range).`, code: `// Try/Catch/Finally\ntry {\n  const data = JSON.parse(rawInput);\n  processData(data);\n} catch (error) {\n  if (error instanceof SyntaxError) {\n    console.error("Invalid JSON:", error.message);\n  } else {\n    throw error; // Re-throw unexpected errors\n  }\n} finally {\n  hideLoadingSpinner();\n}\n\n// Custom error class\nclass NotFoundError extends Error {\n  constructor(resource) {\n    super(\`\${resource} not found\`);\n    this.name = "NotFoundError";\n    this.statusCode = 404;\n  }\n}\n\n// Async error handling\nasync function fetchData(url) {\n  try {\n    const res = await fetch(url);\n    if (!res.ok) throw new NotFoundError(url);\n    return await res.json();\n  } catch (err) {\n    console.error(err.name, err.message);\n  }\n}\n\n// Debugging\nconsole.table(users);          // Pretty table format\nconsole.time("fetch");         // Start timer\nawait fetchData("/api/users");\nconsole.timeEnd("fetch");      // End timer`, codeLabel: 'Error Handling & Debugging', keyPoints: ['Always handle errors — uncaught errors crash your app.', 'Use custom Error classes for categorized error handling.', 'finally is ideal for cleanup (hide spinners, close connections).', 'console.table() and DevTools breakpoints are your best friends.'] },
    { title: 'JavaScript Best Practices', content: `Writing clean, maintainable JavaScript is a skill developed over time. These practices are followed by professional developers worldwide.\n\nUse strict mode. Add "use strict" at the top of files or functions. It catches common mistakes (undeclared variables, duplicate parameters) and makes code more predictable. ES modules use strict mode automatically.\n\nPrefer immutability. Use const by default. When working with arrays and objects, prefer methods that return new values (map, filter, spread) rather than mutating in place (push, sort). Immutable data is easier to reason about and debug.\n\nWrite small, pure functions. A function should do one thing, take inputs, return an output, and have no side effects. This makes functions testable, composable, and reusable. Name them clearly — getActiveUsers() is better than process().\n\nHandle edge cases. What if the array is empty? What if the API returns null? What if the user types unexpected input? Defensive programming prevents crashes. Use optional chaining, nullish coalescing, and input validation.\n\nUse modules to organize code. One file per concern: api.js for HTTP calls, utils.js for helpers, constants.js for magic strings/numbers. Avoid global variables — they create coupling and naming conflicts.\n\nWrite tests. Even basic tests catch regressions that save hours of debugging. Tools: Jest for unit tests, Playwright/Cypress for end-to-end tests. The confidence from tests makes refactoring safe.`, keyPoints: ['Use "use strict" or ES modules for safety.', 'const by default, let when needed, never var.', 'Write small, pure, well-named functions.', 'Handle edge cases (null, empty, unexpected input).', 'Organize code into modules — one concern per file.', 'Write tests — they save more time than they cost.'] }
  ]
};
