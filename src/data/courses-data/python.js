export const pythonCourse = {
  id: 'python', title: 'Python', description: 'High-level language for web, AI, data science, and automation.',
  officialDocs: 'https://docs.python.org/3/', tutorialLink: 'https://www.w3schools.com/python/', exerciseLink: 'https://www.w3schools.com/python/python_exercises.asp',
  sections: [
    { title: 'What is Python', content: `Python is a high-level, interpreted programming language known for its readability and simplicity. Created by Guido van Rossum in 1991, Python's philosophy emphasizes code readability — "There should be one obvious way to do it."\n\nPython uses indentation (whitespace) instead of braces to define code blocks, which enforces clean formatting. It is dynamically typed, garbage collected, and supports multiple programming paradigms: procedural, object-oriented, and functional.\n\nPython dominates in data science (NumPy, Pandas), machine learning (TensorFlow, PyTorch, scikit-learn), web development (Django, Flask, FastAPI), automation/scripting, and DevOps. Its massive standard library ("batteries included") and 400,000+ PyPI packages make it incredibly versatile.`, keyPoints: ['Readability-focused, uses indentation for structure.', 'Dominates AI, data science, and automation.', '"Batteries included" — massive standard library.', 'Dynamically typed, multi-paradigm.'] },
    { title: 'Installation & Environment', content: `Download Python from python.org. Always use Python 3 (Python 2 is end-of-life). Verify installation with python --version and pip --version. Pip is Python's package installer.\n\nVirtual environments isolate project dependencies. Each project gets its own "copy" of installed packages, preventing version conflicts. Use python -m venv venv to create one, then activate it. Always use virtual environments for projects.\n\nPopular IDEs: VS Code with the Python extension (most popular), PyCharm (most feature-rich), Jupyter Notebooks (best for data science and experimentation). For data science, install Anaconda, which bundles Python with scientific packages.`, code: `# Verify installation\npython --version\npip --version\n\n# Create virtual environment\npython -m venv venv\n\n# Activate (Windows)\nvenv\\Scripts\\activate\n# Activate (macOS/Linux)\nsource venv/bin/activate\n\n# Install packages\npip install flask requests pandas\n\n# Save dependencies\npip freeze > requirements.txt\n\n# Restore dependencies\npip install -r requirements.txt\n\n# Project structure\nmy-project/\n├── venv/              # Virtual environment (git-ignored)\n├── app/\n│   ├── __init__.py\n│   ├── routes.py\n│   └── models.py\n├── tests/\n├── requirements.txt\n└── main.py`, codeLabel: 'Environment Setup', keyPoints: ['Always use Python 3 (Python 2 is dead).', 'Virtual environments prevent dependency conflicts.', 'pip freeze > requirements.txt saves dependencies.', 'VS Code + Python extension is the most popular setup.'] },
    { title: 'Core Syntax & Data Types', content: `Python has simple, English-like syntax. Variables don't need type declarations — the interpreter infers types at runtime. No semicolons, no braces — just clean indentation.\n\nBuilt-in types: int (arbitrary precision — no overflow!), float, str (immutable strings), bool (True/False), None (null equivalent). Container types: list (ordered, mutable), tuple (ordered, immutable), dict (key-value pairs), set (unique unordered values).\n\nStrings in Python are extremely powerful: f-strings for interpolation (f"Hello {name}"), slicing (text[0:5]), and dozens of built-in methods (.upper(), .strip(), .split(), .replace()). List comprehensions provide concise syntax for transforming data.`, code: `# Variables (no type declaration needed)\nname = "Alice"\nage = 28\npi = 3.14159\nis_active = True\n\n# Data structures\nfruits = ["apple", "banana", "cherry"]    # List (mutable)\ncoords = (10.5, 20.3)                     # Tuple (immutable)\nuser = {"name": "Alice", "age": 28}       # Dict\nunique_ids = {1, 2, 3, 3}                 # Set: {1, 2, 3}\n\n# f-strings\ngreeting = f"Hello, {name}! You are {age} years old."\n\n# List comprehension\nsquares = [x**2 for x in range(10)]  # [0, 1, 4, 9, 16, ...]\nevens = [x for x in range(20) if x % 2 == 0]\n\n# Dict comprehension\nname_lengths = {n: len(n) for n in fruits}\n\n# Unpacking\nfirst, *rest = fruits  # first="apple", rest=["banana", "cherry"]\n\n# Ternary\nstatus = "adult" if age >= 18 else "minor"`, codeLabel: 'Python Basics', keyPoints: ['No semicolons, no braces — indentation defines blocks.', 'Lists are mutable, tuples are immutable.', 'f-strings are the modern way to format strings.', 'List comprehensions replace verbose loops.'] },
    { title: 'Functions & Classes', content: `Python functions are defined with def. They support default parameters, *args (variable positional arguments as a tuple), **kwargs (variable keyword arguments as a dict), and type hints (optional but recommended).\n\nLambda functions are anonymous one-liners: lambda x: x * 2. They're commonly used with map(), filter(), and sorted(). Decorators (@decorator) wrap functions to extend behavior without modifying them — heavily used in web frameworks.\n\nPython classes use the __init__ method as the constructor, and self as the explicit reference to the instance (unlike this in other languages). Special "dunder" methods (__str__, __repr__, __eq__, __len__) customize object behavior. Dataclasses (@dataclass) reduce boilerplate for data-holding classes.`, code: `# Function with type hints\ndef calculate_bmi(weight: float, height: float) -> float:\n    """Calculate Body Mass Index."""\n    return round(weight / (height ** 2), 1)\n\n# *args and **kwargs\ndef create_user(name, *roles, **metadata):\n    return {"name": name, "roles": roles, **metadata}\n\nuser = create_user("Alice", "admin", "dev", company="Acme")\n\n# Decorator\ndef log_call(func):\n    def wrapper(*args, **kwargs):\n        print(f"Calling {func.__name__}...")\n        result = func(*args, **kwargs)\n        print(f"Done. Returned: {result}")\n        return result\n    return wrapper\n\n@log_call\ndef add(a, b):\n    return a + b\n\n# Dataclass (reduces boilerplate)\nfrom dataclasses import dataclass\n\n@dataclass\nclass Product:\n    name: str\n    price: float\n    quantity: int = 0\n\n    @property\n    def total_value(self):\n        return self.price * self.quantity`, codeLabel: 'Functions & Classes', keyPoints: ['Type hints are optional but strongly recommended.', 'Decorators extend function behavior without modification.', '@dataclass reduces boilerplate for data classes.', '*args for variable args, **kwargs for variable keyword args.'] },
    { title: 'Web Development (Django & FastAPI)', content: `Python has excellent web frameworks. Django is a "batteries included" full-stack framework for large applications. FastAPI is a modern, high-performance framework for building APIs.\n\nDjango follows the MVT (Model-View-Template) pattern. It includes an ORM, admin panel, authentication system, form handling, and much more out of the box. It's ideal for content-heavy applications, CMS systems, and e-commerce platforms.\n\nFastAPI is built on modern Python features (type hints, async/await). It automatically generates OpenAPI documentation, validates request/response data, and handles async operations natively. It's ideal for APIs, microservices, and high-performance backends.\n\nFlask is a lightweight "micro-framework" — it provides routing and request handling but lets you choose your own database, auth, and template solutions. It's ideal for small projects and learning.`, code: `# === FastAPI example ===\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\n\napp = FastAPI()\n\nclass User(BaseModel):\n    name: str\n    email: str\n    age: int\n\nusers_db = []\n\n@app.get("/users")\nasync def get_users():\n    return users_db\n\n@app.post("/users", status_code=201)\nasync def create_user(user: User):\n    users_db.append(user.dict())\n    return user\n\n# Run with: uvicorn main:app --reload\n# Auto docs at: http://localhost:8000/docs\n\n# === Django commands ===\n# django-admin startproject mysite\n# python manage.py startapp blog\n# python manage.py makemigrations\n# python manage.py migrate\n# python manage.py runserver`, codeLabel: 'Web Framework Examples', keyPoints: ['Django: full-featured, "batteries included" for large apps.', 'FastAPI: modern, async, auto-generates API documentation.', 'Flask: lightweight micro-framework for small projects.', 'Use uvicorn for running FastAPI production servers.'] },
    { title: 'Build & Deploy', content: `Python is interpreted — there's no explicit compilation step. However, packaging and deployment require specific tools.\n\nFor web apps, use a production WSGI/ASGI server instead of the built-in development server. Gunicorn (WSGI) for Django/Flask, Uvicorn (ASGI) for FastAPI. Place Nginx in front as a reverse proxy for SSL termination and static file serving.\n\nDocker is the standard deployment method. Create a requirements.txt or use Poetry/Pipenv for dependency management. Multi-stage Docker builds keep images lean.\n\nFor serverless deployment, AWS Lambda and Google Cloud Functions support Python natively. For PaaS deployment, Heroku, Railway, and Render support Python with minimal configuration.`, code: `# requirements.txt\nfastapi==0.104.0\nuvicorn==0.24.0\nsqlalchemy==2.0.23\n\n# Dockerfile\nFROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nEXPOSE 8000\nCMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]\n\n# Deploy with Gunicorn (Django)\ngunicorn mysite.wsgi:application --bind 0.0.0.0:8000 --workers 4\n\n# Deploy with Uvicorn (FastAPI)\nuvicorn main:app --host 0.0.0.0 --port 8000 --workers 4`, codeLabel: 'Deployment Configuration', keyPoints: ['Use Gunicorn/Uvicorn in production, never dev servers.', 'Nginx as reverse proxy for SSL and static files.', 'Docker is the standard modern deployment method.', 'Pin exact dependency versions in requirements.txt.'] }
  ]
};
