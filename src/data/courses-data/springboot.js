export const springBootCourse = { id: 'spring-boot', title: 'Spring Boot', description: 'Java framework for enterprise applications.',
    officialDocs: 'https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/', tutorialLink: 'https://spring.io/guides', exerciseLink: null,
    sections: [
      { title: 'What is Spring Boot', image: '/images/springboot/springboot-architecture.svg', content: `**Spring Boot** is an opinionated framework built on top of the **Spring Framework** that simplifies Java enterprise application development. It eliminates XML configuration, provides **embedded servers** (Tomcat), and auto-configures components based on classpath dependencies.\n\nSpring Boot's philosophy is **"convention over configuration."** It makes reasonable defaults so you can start building features immediately. It is the *de facto standard* for Java microservices and REST APIs in enterprise environments.\n\n### Why Spring Boot\n\nTraditional Spring required extensive XML/Java configuration. Spring Boot auto-detects your dependencies and configures them sensibly. Add \`spring-boot-starter-web\` and you get an embedded Tomcat, JSON serialization, and MVC support — no manual setup. See the [Spring Boot reference](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/) for details.`, keyPoints: ['Eliminates XML boilerplate of traditional Spring.', 'Embedded servers — no external Tomcat needed.', 'Convention over configuration.', 'De facto standard for Java microservices.'] },
      { title: 'Project Setup', content: `Use [Spring Initializr](https://start.spring.io/) to generate a project. Select your build tool (**Maven**/Gradle), Java version, and dependencies (\`Spring Web\`, \`Spring Data JPA\`, \`PostgreSQL Driver\`, etc.).\n\n### Main Application\n\nThe generated project includes a main class annotated with \`@SpringBootApplication\` — this single annotation enables **auto-configuration**, **component scanning**, and Spring configuration.`, code: `// Main Application\n@SpringBootApplication\npublic class MyApp {\n    public static void main(String[] args) {\n        SpringApplication.run(MyApp.class, args);\n    }\n}\n\n// application.yml\nserver:\n  port: 8080\nspring:\n  datasource:\n    url: jdbc:postgresql://localhost:5432/mydb\n    username: user\n    password: pass\n  jpa:\n    hibernate:\n      ddl-auto: update`, codeLabel: 'Project Configuration', keyPoints: ['Use start.spring.io to generate projects.', '@SpringBootApplication enables auto-configuration.', 'application.yml for all configuration.', 'Embedded Tomcat starts on port 8080 by default.'] },
      { title: 'REST Controllers', content: `Controllers handle HTTP requests. \`@RestController\` marks a class as a REST API controller. \`@RequestMapping\` or shortcut annotations (\`@GetMapping\`, \`@PostMapping\`, etc.) map URLs to methods.\n\n### JSON Serialization\n\nSpring automatically serializes return objects to **JSON** using Jackson. \`@RequestBody\` deserializes incoming JSON. \`@PathVariable\` extracts URL segments. \`@RequestParam\` extracts query parameters.`, code: `@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private final UserService userService;\n\n    public UserController(UserService userService) {\n        this.userService = userService; // Constructor injection\n    }\n\n    @GetMapping\n    public List<User> getAll() {\n        return userService.findAll();\n    }\n\n    @GetMapping("/{id}")\n    public User getById(@PathVariable Long id) {\n        return userService.findById(id);\n    }\n\n    @PostMapping\n    @ResponseStatus(HttpStatus.CREATED)\n    public User create(@Valid @RequestBody UserDto dto) {\n        return userService.create(dto);\n    }\n\n    @DeleteMapping("/{id}")\n    @ResponseStatus(HttpStatus.NO_CONTENT)\n    public void delete(@PathVariable Long id) {\n        userService.delete(id);\n    }\n}`, codeLabel: 'REST API Controller', keyPoints: ['@RestController = @Controller + @ResponseBody.', 'Constructor injection for dependencies.', '@Valid triggers bean validation on request body.', 'Spring auto-serializes objects to JSON.'] },
      { title: 'Spring Data JPA', content: `**Spring Data JPA** simplifies database access by generating repository implementations from interfaces. Define an entity class with \`@Entity\`, then create a repository interface extending \`JpaRepository\` — Spring generates all CRUD methods automatically.\n\n### Custom Queries\n\nCustom queries can be derived from method names (\`findByEmail\`, \`findByAgeGreaterThan\`) or written explicitly with \`@Query\`.`, code: `@Entity\n@Table(name = "users")\npublic class User {\n    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n    private String name;\n    @Column(unique = true)\n    private String email;\n    // Getters, setters, constructors\n}\n\npublic interface UserRepository extends JpaRepository<User, Long> {\n    Optional<User> findByEmail(String email);\n    List<User> findByNameContainingIgnoreCase(String name);\n    @Query("SELECT u FROM User u WHERE u.age BETWEEN :min AND :max")\n    List<User> findByAgeRange(@Param("min") int min, @Param("max") int max);\n}`, codeLabel: 'JPA Entity & Repository', keyPoints: ['JpaRepository provides CRUD without writing SQL.', 'Method names auto-generate queries.', '@Query for custom JPQL/SQL queries.', '@Entity maps a class to a database table.'] },
      { title: 'Service Layer & Dependency Injection', content: `The **Service layer** contains business logic, sitting between controllers and repositories. It keeps controllers thin and repositories focused on data access.\n\n### Dependency Injection\n\nSpring's **IoC container** manages object creation and wiring. Use \`@Service\`, \`@Repository\`, \`@Component\` to register beans. Inject dependencies via **constructor injection** (recommended) — Spring auto-wires matching beans.\n\n### Transaction Management\n\n\`@Transactional\` ensures database operations are atomic. If any operation fails, all changes are **rolled back** automatically.`, code: `@Service\npublic class UserService {\n    private final UserRepository userRepo;\n    private final EmailService emailService;\n\n    public UserService(UserRepository userRepo, EmailService emailService) {\n        this.userRepo = userRepo;\n        this.emailService = emailService;\n    }\n\n    @Transactional\n    public User createUser(CreateUserDto dto) {\n        if (userRepo.findByEmail(dto.getEmail()).isPresent()) {\n            throw new DuplicateEmailException(dto.getEmail());\n        }\n        User user = new User(dto.getName(), dto.getEmail());\n        User saved = userRepo.save(user);\n        emailService.sendWelcome(saved.getEmail());\n        return saved;\n    }\n\n    public List<User> findAll() {\n        return userRepo.findAll();\n    }\n\n    public User findById(Long id) {\n        return userRepo.findById(id)\n            .orElseThrow(() -> new NotFoundException("User not found"));\n    }\n}`, codeLabel: 'Service Layer', keyPoints: ['@Service for business logic beans.', 'Constructor injection is the recommended DI approach.', '@Transactional ensures atomic database operations.', 'Services keep controllers thin and focused.'] },
      { title: 'Build & Deploy', content: `\`mvn clean package\` creates an **executable JAR** with embedded Tomcat. Run with \`java -jar target/app.jar\`. For Docker, use multi-stage builds with a **JRE-only** runtime image.\n\n### Spring Profiles\n\nSpring profiles (\`application-dev.yml\`, \`application-prod.yml\`) manage environment-specific configuration. Activate with \`--spring.profiles.active=prod\`.`, code: `# Build\nmvn clean package -DskipTests\njava -jar target/my-app-1.0.0.jar --spring.profiles.active=prod\n\n# Dockerfile\nFROM eclipse-temurin:21-jdk-alpine AS build\nWORKDIR /app\nCOPY . .\nRUN ./mvnw clean package -DskipTests\n\nFROM eclipse-temurin:21-jre-alpine\nCOPY --from=build /app/target/*.jar app.jar\nEXPOSE 8080\nCMD ["java", "-jar", "app.jar"]`, codeLabel: 'Build & Deploy', keyPoints: ['Executable JAR includes embedded Tomcat.', 'Spring profiles manage environment configs.', 'Docker multi-stage with JRE-only for production.', 'mvn clean package builds and tests everything.'] },
      { title: 'Best Practices & Code Standards', content: `Follow the **layered architecture**: Controllers handle HTTP concerns, Services contain business logic, and Repositories manage data access. Each layer depends only on the layer below it. This separation makes components testable in isolation and easy to replace.\n\n### Dependency Injection\n\nUse **constructor injection** exclusively — never field injection with \`@Autowired\`. Constructor injection makes dependencies explicit, enables immutability (\`final\` fields), and simplifies unit testing. Spring auto-detects constructor injection when there is only one constructor.\n\n### Error Handling & Configuration\n\nUse \`@ControllerAdvice\` for **centralized exception handling** instead of try-catch in every controller method. This ensures consistent error responses across all endpoints. Use **Spring Profiles** (\`application-dev.yml\`, \`application-prod.yml\`) to manage environment-specific configuration and never hardcode secrets.`, code: `// DO: Constructor injection with final fields\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private final UserService userService;\n\n    public UserController(UserService userService) {\n        this.userService = userService;\n    }\n}\n\n// DON'T: Field injection (hard to test)\n@RestController\npublic class UserController {\n    @Autowired private UserService userService;\n}\n\n// DO: Centralized exception handling\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(NotFoundException.class)\n    @ResponseStatus(HttpStatus.NOT_FOUND)\n    public ErrorResponse handleNotFound(NotFoundException ex) {\n        return new ErrorResponse(ex.getMessage());\n    }\n\n    @ExceptionHandler(Exception.class)\n    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)\n    public ErrorResponse handleGeneral(Exception ex) {\n        return new ErrorResponse("An unexpected error occurred");\n    }\n}\n\n// DON'T: Try-catch in every controller method\n\n// DO: Use profiles for environment config\n# application-dev.yml\nspring.datasource.url: jdbc:h2:mem:testdb\n# application-prod.yml\nspring.datasource.url: jdbc:postgresql://prod:5432/mydb`, codeLabel: 'Dos & Don\'ts', keyPoints: ['Use constructor injection, never field injection with @Autowired.', 'Separate concerns: Controller (HTTP), Service (logic), Repository (data).', 'Use @ControllerAdvice for centralized exception handling.', 'Spring Profiles manage environment-specific configuration cleanly.'] }
    ]
  };
