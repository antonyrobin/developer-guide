export const systemArchitectureCourse = {
  id: 'sys-arch', title: 'System Architecture', description: 'Designing scalable, reliable software systems.',
  officialDocs: null, tutorialLink: 'https://github.com/donnemartin/system-design-primer', exerciseLink: null,
  sections: [
    { title: 'Architecture Patterns', image: '/images/system-architecture/architecture-patterns.svg', content: `### Monolithic Architecture\n\n**Monolithic**: All code in one deployable unit. *Simple* to develop and deploy initially, but harder to scale and maintain as it grows. Teams must coordinate releases, and a bug in one module can crash the entire system.\n\n### Microservices Architecture\n\n**Microservices**: System split into small, independent services communicating via **APIs**. Each service is deployed, scaled, and maintained independently. Requires investment in *service discovery*, *distributed tracing*, and *inter-service communication*.\n\n### Event-Driven Architecture\n\n**Event-Driven**: Services communicate through events/messages (\`Kafka\`, \`RabbitMQ\`) rather than direct API calls. Enables **loose coupling** and *async processing*. Great for *real-time analytics*, *order processing*, and *notification systems*.\n\n### Serverless Architecture\n\n**Serverless**: Cloud provider manages infrastructure entirely. Deploy functions (*AWS Lambda*, *Azure Functions*) that **auto-scale** and charge per execution. Ideal for *event-driven*, *bursty workloads* with unpredictable traffic.`, keyPoints: ['Monolith: simple start, hard to scale.', 'Microservices: independent, scalable, complex.', 'Event-driven: loose coupling via message queues.', 'Serverless: zero infrastructure management.'] },
    { title: 'Scalability & Load Balancing', content: `**Vertical scaling** adds more power to one server (more CPU/RAM). **Horizontal scaling** adds more servers. Horizontal scaling is preferred because it has *no upper limit* and provides **redundancy**.\n\n### Load Balancing\n\n**Load balancers** distribute incoming traffic across multiple servers. Algorithms: *Round Robin*, *Least Connections*, *IP Hash*. **Health checks** automatically remove unhealthy servers from rotation. Popular tools include **Nginx**, **HAProxy**, and cloud-native ALBs.\n\n### Caching & CDNs\n\n**Caching** (\`Redis\`, \`Memcached\`) stores frequently accessed data in memory, reducing database load by orders of magnitude. **CDNs** (*Content Delivery Networks*) like **CloudFront** and **Cloudflare** cache static assets globally for faster delivery to users worldwide.`, keyPoints: ['Horizontal scaling > vertical scaling.', 'Load balancers distribute traffic across servers.', 'Caching reduces database load dramatically.', 'CDNs deliver static assets globally.'] },
    { title: 'CAP Theorem & Database Scaling', content: `The **CAP Theorem** states a distributed system can guarantee only **2 of 3**: **Consistency** (every read returns the latest write), **Availability** (every request gets a response), and **Partition Tolerance** (system works despite network failures).\n\n### Database Scaling Strategies\n\n**Read replicas** handle read traffic separately from writes — essential for *read-heavy* workloads. **Sharding** splits data across multiple database instances by a key (\`user_id\`, \`region\`). **Connection pooling** (\`PgBouncer\`, \`HikariCP\`) reuses database connections to prevent exhaustion.\n\n### Choosing the Right Strategy\n\nStart with **vertical scaling** and **read replicas** before introducing sharding complexity. Use **eventual consistency** where real-time accuracy isn't critical (analytics, feeds). Design for failure — assume *any component can crash at any time*.`, keyPoints: ['CAP: choose 2 of Consistency, Availability, Partition Tolerance.', 'Read replicas for scaling read-heavy workloads.', 'Sharding distributes data across multiple databases.', 'Design for failure — assume any component can crash.'] },
    { title: 'API Gateway & Middleware', image: '/images/system-architecture/api-gateway.png', content: `An **API Gateway** is a server that acts as an API front-end, receiving requests, enforcing policies, and routing them to the appropriate backend services.\n\n### Key Gateway Features\n\n**Authentication & Authorization**: Centralized security check before requests reach services. **Rate Limiting**: Protects backends from being overwhelmed by limiting requests per IP/User. **Caching**: Stores responses for frequently requested resources to reduce latency.\n\n**Versioning**: Manages multiple API versions (e.g., \`/v1/users\`, \`/v2/users\`) for backward compatibility. **Logging & Observability**: Centralized request/response logging and performance metrics. **Resilience**: Implements patterns like **Circuit Breakers** and **Retries** using libraries like **Polly** (C#) or **Resilience4j** (Java).`, code: `// C# - API Gateway Configuration (YARP/Ocelot)\n"Routes": {\n  "user-route": {\n    "ClusterId": "user-service",\n    "Match": { "Path": "/api/users/{**remainder}" },\n    "Transforms": [ { "PathRemovePrefix": "/api" } ]\n  }\n}\n\n// Java - Spring Cloud Gateway\n@Bean\npublic RouteLocator customRouteLocator(RouteLocatorBuilder builder) {\n  return builder.routes()\n    .route("user_route", r -> r.path("/api/users/**")\n      .filters(f -> f.stripPrefix(1)\n        .requestRateLimiter(c -> c.setRateLimiter(myRateLimiter())))\n      .uri("lb://user-service"))\n    .build();\n}`, codeLabel: 'Gateway Examples', keyPoints: ['Centralized entry point for all client requests.', 'Enforces security, rate limiting, and CORS policies.', 'Manages API versioning and request routing.', 'Implements resilience patterns like Circuit Breakers.'] },
    { title: 'Authentication Methods', content: `Choosing the right authentication method depends on your system's security requirements and architecture.\n\n1. **API Keys**: Static keys sent in headers. Simple for app-to-app but risky if leaked.\n2. **Basic Auth**: Username/Password in Base64. Easy but requires HTTPS and lacks session control.\n3. **Bearer Tokens / JWT**: Signed tokens carrying user claims. Stateless and scalable, ideal for microservices.\n4. **OAuth 2.0 / OIDC**: Industry standards for delegated access and identity layers.\n5. **HMAC**: Request signatures for high security, ensuring message integrity.\n6. **Mutual TLS (mTLS)**: Certificate-based auth where both client and server prove identity. Essential for zero-trust service-to-service communication.`, keyPoints: ['JWT is the standard for modern stateless APIs.', 'OAuth 2.0 handles authorization; OIDC handles identity.', 'mTLS provides the strongest service-to-service security.', 'Always use HTTPS regardless of the auth method.'] },
    { title: 'API Design & Communication Patterns', content: `### REST APIs\n\n**REST** (*Representational State Transfer*) is the most common API style. Resources are accessed via **HTTP methods**: \`GET\` (read), \`POST\` (create), \`PUT\` (update), \`DELETE\` (remove). Use *plural nouns* for endpoints: \`/api/users\`, \`/api/orders/:id\`. Return proper **HTTP status codes**: \`200\` success, \`201\` created, \`400\` bad request, \`404\` not found, \`500\` server error.\n\n### GraphQL\n\n**GraphQL** lets clients request *exactly the data they need* in a single query — no over-fetching or under-fetching. Ideal for *complex UIs* with varied data needs. Trade-off: more complex backend setup and caching.\n\n### gRPC & Message Queues\n\n**gRPC** uses *Protocol Buffers* for fast, strongly-typed service-to-service communication. **Message queues** (\`RabbitMQ\`, \`Kafka\`, \`SQS\`) enable **asynchronous communication** — the sender doesn't wait for the receiver. Essential for *decoupled*, *event-driven* architectures.\n\n ### SOAP\n **SOAP** is XML-based, heavy but still used in legacy enterprise systems for its strict security standards.\n\n ### Real-Time: WebSockets, SSE & MQTT\n**WebSockets** provide full-duplex communication for chat/gaming. **Server-Sent Events (SSE)** are one-way (server to client) for feeds. **MQTT** is a lightweight pub/sub protocol optimized for IoT devices.`, code: `// REST API Design Example\nGET    /api/users          // List all users\nGET    /api/users/:id       // Get single user\nPOST   /api/users           // Create user\nPUT    /api/users/:id       // Update user\nDELETE /api/users/:id       // Delete user\n\n// Status Codes\n200 OK              // Successful read\n201 Created         // Successful create\n204 No Content      // Successful delete\n400 Bad Request     // Invalid input\n401 Unauthorized    // Not authenticated\n403 Forbidden       // Not authorized\n404 Not Found       // Resource doesn't exist\n429 Too Many Req    // Rate limited\n500 Internal Error  // Server error\n\n// GraphQL Query Example\nquery {\n  user(id: "123") {\n    name\n    email\n    orders {\n      total\n      status\n    }\n  }\n}`, codeLabel: 'API Design Patterns', keyPoints: ['REST: HTTP methods + plural nouns + status codes.', 'GraphQL: client-driven queries, no over-fetching.', 'gRPC: fast binary protocol for service-to-service.', 'Message queues enable async, decoupled communication.'] },
    { title: 'API Design Best Practices', content: `Build APIs that are intuitive, scalable, and secure by following these industry-standard practices.\n\n### 1. Resource Naming & Plurals\nUse clear nouns and maintain consistency with plurals: \`GET /users/123/orders\` instead of \`GET /user/123/order\`. Interlink related resources logically.\n\n### 2. Idempotency\nEnsure that multiple identical requests have the same effect as a single request. This is crucial for \`PUT\` and \`DELETE\` operations to handle network retries safely.\n\n### 3. Versioning & Pagination\nVersion your APIs (URL, Header, or Query) to avoid breaking changes. Always paginate large datasets (\`GET /posts?page=1&size=20\`) to improve performance and user experience.\n\n### 4. Proper Status Codes\nReturn semantic HTTP status codes: \`201 Created\` for success, \`400 Bad Request\` for validation errors, \`401 Unauthorized\` for auth issues, and \`429 Too Many Requests\` for rate limiting.`, code: `// Good API Design\nGET    /api/v1/posts?page=2&size=50  // Paginated List\nGET    /api/v1/posts/456             // Single Resource\nPOST   /api/v1/posts/456/comments    // Nested Resource\n\n// Idempotent PUT\nPUT    /api/v1/users/123/profile\n{\n  "name": "Antony",\n  "bio": "Software Engineer"\n}\n// Result is the same regardless of how many times it is sent.`, codeLabel: 'API Best Practices', keyPoints: ['Use plural nouns for resource names.', 'Ensure operations are idempotent where appropriate.', 'Version APIs to maintain backward compatibility.', 'Always paginate large lists to protect performance.'] },
    { title: 'Monitoring, Reliability & Incident Response', content: `### Observability Pillars\n\nProduction systems need three pillars of observability: **Logs** (what happened), **Metrics** (how the system is performing), and **Traces** (request flow across services). Tools like *Prometheus*, *Grafana*, *Datadog*, and *ELK Stack* provide this visibility.\n\n### Reliability Engineering\n\n**SLOs** (*Service Level Objectives*) define target reliability — e.g., *99.9% uptime*. **Error budgets** balance reliability with feature velocity. **Circuit breakers** prevent cascading failures by stopping calls to failing services. **Retry with exponential backoff** handles transient errors gracefully.\n\n### Incident Response\n\nWhen systems fail: **detect** (alerts, dashboards), **respond** (on-call rotation, runbooks), **mitigate** (rollback, feature flags), **review** (blameless postmortems). Great teams invest in *automation*, *runbooks*, and *game days* to practice failure scenarios before they hit production.`, keyPoints: ['Three pillars: Logs, Metrics, Traces.', 'SLOs and error budgets balance reliability with velocity.', 'Circuit breakers prevent cascading failures.', 'Blameless postmortems improve systems, not blame people.'] },
    { title: 'Best Practices & Code Standards', content: `**Start with a monolith** and extract microservices only when you have clear evidence that a component needs independent scaling, deployment, or team ownership. Premature microservices add enormous complexity (networking, data consistency, debugging) without proven benefits.\n\n### Document Decisions\n\nUse **Architecture Decision Records (ADRs)** to document every significant technical decision — what was chosen, why, what alternatives were considered, and what the consequences are. Future team members will thank you. Store ADRs in version control alongside the code.\n\n### Security & Reliability\n\nApply **security at every layer**: HTTPS everywhere, authentication on all endpoints, rate limiting, input validation at system boundaries, secrets in a vault (never in code), and regular dependency vulnerability scans. **Design for failure** — assume any component can crash. Implement health checks, circuit breakers, retries with backoff, and rollback strategies.`, code: `# Architecture Decision Record (ADR) Template\n\n## ADR-001: Use PostgreSQL as Primary Database\nDate: 2024-01-15\nStatus: Accepted\n\n## Context\nE-commerce platform with 10K users, 50K daily transactions.\nNeed ACID compliance for financial data.\n\n## Decision\nPostgreSQL with read replicas for the product catalog.\n\n## Reasons\n- ACID compliance for transactions\n- Read replicas handle catalog read load\n- Mature ecosystem and tooling\n\n## Consequences\n- Team needs PostgreSQL expertise\n- Must implement connection pooling\n- Migration to NoSQL would require significant effort\n\n# Production Readiness Checklist\n- [ ] HTTPS/TLS on all endpoints\n- [ ] Authentication and authorization\n- [ ] Rate limiting on public APIs\n- [ ] Input validation at every boundary\n- [ ] Secrets stored in vault (not in code)\n- [ ] Health check endpoints configured\n- [ ] Monitoring, logging, and alerting\n- [ ] Disaster recovery plan tested\n- [ ] Load testing completed\n- [ ] Runbooks for common incidents`, codeLabel: 'ADR Template & Checklist', keyPoints: ['Start with a monolith — microservices add complexity prematurely.', 'Document every major decision with Architecture Decision Records.', 'Apply security at every layer: network, application, and data.', 'Design for failure — health checks, circuit breakers, and rollbacks.'] }

  ]
};
