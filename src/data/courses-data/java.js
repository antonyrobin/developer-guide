export const javaCourse = {
  id: 'java', title: 'Java', description: 'Enterprise-grade, platform-independent programming language.',
  officialDocs: 'https://docs.oracle.com/en/java/', tutorialLink: 'https://www.w3schools.com/java/', exerciseLink: 'https://www.w3schools.com/java/java_exercises.asp',
  sections: [
    { title: 'What is Java', content: `Java is a high-level, class-based, object-oriented programming language designed for portability — "Write Once, Run Anywhere" (WORA). Java source code compiles into bytecode that runs on the Java Virtual Machine (JVM), making it platform-independent.\n\nCreated by James Gosling at Sun Microsystems in 1995, Java became the backbone of enterprise software. It powers Android apps, banking systems, e-commerce platforms, big data tools (Hadoop, Spark), and millions of backend services worldwide.\n\nJava is statically typed (types are checked at compile time), strongly typed (no implicit dangerous conversions), garbage collected (memory is managed automatically), and multi-threaded (built-in support for concurrent programming). Modern Java (17+) has introduced records, pattern matching, sealed classes, and virtual threads, making it much more concise than legacy Java.`, image: 'https://images.unsplash.com/photo-1517694712202-14dd9538aa97?w=800&auto=format&fit=crop&q=60', keyPoints: ['Write Once, Run Anywhere — platform-independent via JVM.', 'Statically typed, garbage collected, multi-threaded.', 'Dominant in enterprise, Android, and big data.', 'Modern Java (17+) is significantly more concise.'] },
    { title: 'Installation & Setup', content: `To develop Java, you need the Java Development Kit (JDK). The JDK includes the compiler (javac), the runtime (JRE/JVM), and development tools. Use the latest LTS (Long-Term Support) version — currently Java 21.\n\nDownload from Adoptium (adoptium.net), Oracle, or Amazon Corretto. After installation, verify by running java --version and javac --version in your terminal.\n\nFor IDE, IntelliJ IDEA (Community Edition is free) is the industry standard. It provides intelligent code completion, refactoring, debugging, and Maven/Gradle integration. Eclipse and VS Code are alternatives.\n\nJava projects use build tools: Maven (XML-based, convention over configuration) or Gradle (Groovy/Kotlin-based, more flexible). These handle dependency management, compilation, testing, and packaging.`, code: `# Install Java (macOS with Homebrew)\nbrew install openjdk@21\n\n# Verify installation\njava --version\njavac --version\n\n# Create a Maven project\nmvn archetype:generate \\\n  -DgroupId=com.myapp \\\n  -DartifactId=my-project \\\n  -DarchetypeArtifactId=maven-archetype-quickstart\n\n# Project structure\nmy-project/\n├── pom.xml              # Maven config & dependencies\n└── src/\n    ├── main/java/       # Source code\n    └── test/java/       # Test code`, codeLabel: 'Setup Commands', keyPoints: ['Use the latest LTS version (Java 21).', 'IntelliJ IDEA is the recommended IDE.', 'Maven or Gradle for build and dependency management.', 'Verify with java --version and javac --version.'] },
    { title: 'Program Structure & Hello World', content: `Every Java program starts with a class. The filename must match the public class name exactly (case-sensitive). The main method is the entry point: public static void main(String[] args).\n\nJava is organized into packages (like folders for classes). The package declaration goes at the top, followed by imports, then the class definition. Packages prevent naming conflicts and organize related classes logically.\n\nJava is strictly typed — every variable must declare its type. Since Java 10, you can use var for local variable type inference when the type is obvious from the right side.`, code: `package com.quickdevguide;\n\nimport java.util.List;\nimport java.util.ArrayList;\n\npublic class App {\n    public static void main(String[] args) {\n        // Variable declarations\n        String name = "QuickDev";\n        int count = 42;\n        var items = new ArrayList<String>(); // Type inference (Java 10+)\n\n        items.add("HTML");\n        items.add("CSS");\n        items.add("Java");\n\n        // Enhanced for loop\n        for (String item : items) {\n            System.out.println("Course: " + item);\n        }\n\n        // Stream API (Java 8+)\n        items.stream()\n             .filter(i -> i.length() > 3)\n             .map(String::toUpperCase)\n             .forEach(System.out::println);\n    }\n}`, codeLabel: 'Java Program Structure', keyPoints: ['Filename must match public class name.', 'main() is the program entry point.', 'Use packages to organize related classes.', 'var enables type inference for local variables (Java 10+).'] },
    { title: 'Data Types & Collections', content: `Java has 8 primitive types: byte (8-bit), short (16-bit), int (32-bit), long (64-bit), float (32-bit decimal), double (64-bit decimal), boolean, and char. Primitives are stored on the stack and are very fast.\n\nWrapper classes (Integer, Double, Boolean, etc.) box primitives into objects for use with generics and collections. Autoboxing/unboxing converts between them automatically.\n\nThe Collections Framework provides powerful data structures: List (ordered, duplicates allowed — ArrayList for random access, LinkedList for frequent insertions), Set (unique elements — HashSet for speed, TreeSet for sorted), Map (key-value pairs — HashMap for speed, TreeMap for sorted keys), Queue/Deque for FIFO/LIFO.\n\nSince Java 9, immutable collections can be created with factory methods: List.of(), Set.of(), Map.of(). These are thread-safe and memory-efficient.`, code: `// Primitives vs Objects\nint count = 42;              // Primitive (fast, stack)\nInteger boxed = count;       // Autoboxing to object\n\n// Collections\nList<String> names = new ArrayList<>();\nnames.add("Alice");\nnames.add("Bob");\n\nMap<String, Integer> scores = new HashMap<>();\nscores.put("Alice", 95);\nscores.put("Bob", 87);\n\nSet<String> unique = new HashSet<>(names);\n\n// Immutable collections (Java 9+)\nvar colors = List.of("Red", "Blue", "Green");\nvar config = Map.of("host", "localhost", "port", "8080");\n\n// Stream operations\ndouble avg = scores.values().stream()\n    .mapToInt(Integer::intValue)\n    .average()\n    .orElse(0.0);`, codeLabel: 'Types & Collections', keyPoints: ['8 primitive types, each with a wrapper class.', 'ArrayList for random access, LinkedList for insertions.', 'HashMap for fast key-value lookups.', 'Use List.of(), Map.of() for immutable collections.'] },
    { title: 'OOP in Java', content: `Java is fundamentally object-oriented. Everything exists inside a class (except primitives). Java supports single inheritance (extends one class) and multiple interface implementation (implements multiple interfaces).\n\nAccess modifiers control visibility: public (accessible everywhere), protected (same package + subclasses), default/package-private (same package only), private (same class only). Use the most restrictive access level possible.\n\nInterfaces define contracts — what a class must do, but not how. Since Java 8, interfaces can contain default methods (with implementation) and static methods. Since Java 17, sealed classes restrict which classes can extend them.\n\nAbstract classes provide partial implementation — some methods are concrete, others are abstract (must be implemented by subclasses). Use abstract classes when subclasses share significant code; use interfaces when they share only a contract.`, code: `// Interface\npublic interface Printable {\n    void print();\n    default String format() { return toString(); } // Default method\n}\n\n// Abstract class\npublic abstract class Shape {\n    protected String color;\n    public Shape(String color) { this.color = color; }\n    public abstract double area(); // Must be implemented\n    public String describe() { return color + " shape"; }\n}\n\n// Concrete class\npublic class Circle extends Shape implements Printable {\n    private final double radius;\n\n    public Circle(String color, double radius) {\n        super(color);\n        this.radius = radius;\n    }\n\n    @Override\n    public double area() { return Math.PI * radius * radius; }\n\n    @Override\n    public void print() { System.out.println(describe()); }\n}`, codeLabel: 'Java OOP Patterns', keyPoints: ['Single inheritance + multiple interface implementation.', 'Use the most restrictive access modifier possible.', 'Interfaces = contracts. Abstract classes = partial implementation.', 'Sealed classes (Java 17+) restrict inheritance hierarchies.'] },
    { title: 'Exception Handling', content: `Java has a robust exception handling system. Exceptions are objects that represent runtime errors. They fall into two categories: Checked exceptions (must be caught or declared with "throws") and Unchecked exceptions (RuntimeException subclasses, don't need explicit handling).\n\nThe try-catch-finally block is the primary mechanism. Try contains risky code, catch handles specific exception types, finally runs regardless (for cleanup). Java 7+ supports try-with-resources, which automatically closes resources like database connections and file streams.\n\nBest practices: Catch specific exceptions, not generic Exception. Never swallow exceptions silently (empty catch blocks). Use custom exceptions to model business-specific errors. Log exceptions with context (what operation failed, what data was involved).`, code: `// Try-with-resources (auto-closes resources)\ntry (var reader = new BufferedReader(new FileReader("data.csv"))) {\n    String line;\n    while ((line = reader.readLine()) != null) {\n        processLine(line);\n    }\n} catch (FileNotFoundException e) {\n    logger.error("File not found: " + e.getMessage());\n} catch (IOException e) {\n    logger.error("IO error: " + e.getMessage());\n}\n\n// Custom exception\npublic class InsufficientFundsException extends RuntimeException {\n    private final double balance;\n    private final double amount;\n\n    public InsufficientFundsException(double balance, double amount) {\n        super(String.format("Cannot withdraw %.2f from balance %.2f", amount, balance));\n        this.balance = balance;\n        this.amount = amount;\n    }\n}`, codeLabel: 'Exception Handling', keyPoints: ['Checked exceptions MUST be caught or declared.', 'Use try-with-resources for auto-closing resources.', 'Catch specific exceptions, never generic Exception.', 'Create custom exceptions for business logic errors.'] },
    { title: 'Build & Deploy', content: `Java projects are built using Maven or Gradle, which compile source code, run tests, and package everything into deployable artifacts.\n\nMaven uses a pom.xml file to declare dependencies, plugins, and build configurations. Running mvn clean package compiles the code, runs tests, and produces a JAR or WAR file in the target/ directory.\n\nA JAR (Java Archive) is a ZIP file containing compiled .class files and resources. A "fat JAR" (uber-JAR) includes all dependencies, making it self-contained. Spring Boot applications typically produce executable fat JARs.\n\nFor deployment, you can: 1) Run the JAR directly on a server (java -jar app.jar), 2) Deploy to application servers like Tomcat or WildFly (for WAR files), 3) Containerize with Docker (most modern approach), or 4) Deploy to cloud PaaS services (AWS Elastic Beanstalk, Azure App Service).`, code: `<!-- pom.xml (Maven) -->\n<project>\n  <groupId>com.myapp</groupId>\n  <artifactId>my-service</artifactId>\n  <version>1.0.0</version>\n  <packaging>jar</packaging>\n\n  <dependencies>\n    <dependency>\n      <groupId>org.springframework.boot</groupId>\n      <artifactId>spring-boot-starter-web</artifactId>\n      <version>3.2.0</version>\n    </dependency>\n  </dependencies>\n</project>\n\n# Build commands\nmvn clean package          # Compile + test + package\nmvn clean package -DskipTests  # Skip tests\njava -jar target/my-service-1.0.0.jar  # Run\n\n# Dockerfile\nFROM eclipse-temurin:21-jre-alpine\nCOPY target/my-service-1.0.0.jar app.jar\nEXPOSE 8080\nCMD ["java", "-jar", "app.jar"]`, codeLabel: 'Build & Deploy Configuration', keyPoints: ['Maven: mvn clean package produces a JAR/WAR.', 'Fat JARs include all dependencies for self-contained deployment.', 'Docker is the modern standard for Java deployment.', 'Always run tests as part of the build process.'] }
  ]
};
